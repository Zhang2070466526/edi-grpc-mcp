# EDI MCP 服务评审报告（待办问题清单）

- **评审日期**：2026-09-08（持续更新至 09-10）
- **评审对象**：EDI MCP 服务（`http://127.0.0.1:50026/mcp`，`D:\GitLabCode\edi-grpc-mcp\start_servers.py`），86 个业务工具；第八节为代码级过度工程审计（对象=仓库源码本身）
- **评审方法**：① 运行形态侦察（netstat/进程命令行/`/ready`）；② 全量 `tools/list` 定义审查（name/description/inputSchema 静态分析）；③ 行为抽样探针（直接调工具 + 白名单探针裸调 `/mcp`）；④ 读真实代码核对契约
- **说明**：本清单只保留**未解决**项（已修复项已移除）。

---

## 一、运行形态快照（评审时点）

| 项 | 值 |
|---|---|
| 50026 进程 | uv python 3.14 直跑 `start_servers.py`（开发源码形态，进程白名单鉴权） |
| `/ready` | `tool_count=86`、`grpc=online`、`tools_hash=1e5c9305`、`version=0.1.8`、`stateless=true` |
| 真实 `tools/list` | 86 条；前缀分布：`tr_`×17、`get_`×13、`list_`×6、`cst_`×5、`simulate_`×3、`search_`×4 … |
| 协议能力 | `prompts/list` 9 个、`resources/list` 7 个（实测可用） |
| EDI.exe (50055) | 运行中，LISTENING |
| SimulationAgent (17866/17867) | 运行中，`channel_state=ready` |

---

## 二、P0（高优先级）

### P0-1 🔐 SimulationAgent 密钥裸奔在进程命令行
- **现象**：`SimulationAgent.exe` 以 `--model-api-key sk-...` 与 `--edi-mms-api-token <JWT>` 作为 argv 明文启动（两个进程 29092/35608 均带）。
- **影响**：本机任何进程/用户可 `ps`/WMI 读到明文密钥。
- **说明**：本仓库（edi-grpc-mcp）代码内无硬编码密钥、`.env` 已被 `.gitignore` 忽略（第9行）——此问题是 EDI 侧 `SimulationAgent.exe` 的启动参数，修复位置不在本仓库。
- **建议**：改从环境变量/配置文件读取；启动后清零 argv。

---

## 三、P1（契约 / 命名不一致）

| # | 问题 | 证据 / 说明 |
|---|---|---|
| P1-1 | 「型号/ID 列表」参数名不统一 | `batch_query_component.model_name_list`（仅型号串）／`tr_query_components.originalid_list`（UUID 或 model_name，未改）／`get_components_static_params.original_uuids`（真 UUID）——三处仍不统一 |
| P1-2 | `originalid` 命名残留 | `tr_query_components` 仍用 `originalid_list`（无下划线），建议一并改名对齐 batch_query |
| P1-3 | 工程路径多名字 | `list_epp_projects.folder_path` vs `create_project.path` vs 多数 `project_path` vs tr_* `epp_path`（镜像 SimulationAgent 契约，保留） |

---

## 四、P2（文档 / 质量）

- **P2-1　docstring 格式不统一**：多数「用法:」中文体，少数「Args:」英文体（`close_edi_project`、`capture_schematic`、`simulate_netlist_with_ads` 等）。
- **P2-2　返回字段键语言不一致**：`get_components_static_params` 中文键（封装/尺寸/成本/所属厂商/模型id/重量），`batch_query_component` 英文键（model/manufacturer/type）+ 中文 `specs` 串。
- **P2-3　功能冗余 + 返回信封不一致**：`list_schematic_components` / `list_simulation_components` / `tr_query_schematic_components` 高度重叠，且前两者**返回信封不同**——`list_schematic_components` 是 gRPC 任务信封（task_id/status/details.component_count）、**无分页**；`list_simulation_components` 是扁平结构（total/offset/limit/component_type_counts）、**带 offset/limit 分页**。同一批器件两种信封、一个能翻页一个不能，调用方二选一很纠结。docstring 应点明各自定位与取舍。
- **P2-4　协议方法暴露为「伪工具」（备忘，非 bug）**：`get_prompt`/`list_prompts`/`list_resources`/`read_resource` 是 MCP 协议方法（非工具），服务端实际可用（实测 `prompts/list` 9 个、`resources/list` 7 个、`prompts/get` 正常）。Hermes 会话快照把它们暴露成 4 个「伪工具」，经 `tool_call` 调会失败——纯客户端展示问题，记录备忘。
- **P2-5　CSV 导出 EXCLUDED_TYPES 漏了 `Out`**：工程 23 共 9 器件，`export_schematic_components_to_csv` 正确排除了 7 个仿真控制器/端口（TermG / P_nToneG / SParameter / Sweep / Var / HarmonicBalance / XDB），但**输出端口标记 `Out` 漏进了导出**（CSV 出了 `Out,Out1,,,` 一行）。按 docstring「端口不导出」口径，`Out` 也应入 EXCLUDED_TYPES。

---

## 五、优化建议（服务端视角，按落地优先级）

1. **统一 `originalid_list` 语义**（P1-1/P1-2）：要么都认 UUID，要么把「只认型号串」写进 docstring 并加校验拦截静默丢弃。（P1-4 `tr_query_components` 静默丢弃已修复：已按 model_name 对齐并补 null+missing。）
2. **密钥治理**（P0-1）：密钥走环境变量/配置，不落 argv，启动后清零（EDI 侧修复）。
3. **命名收敛**（P1-1~P1-3）：`tr_query_components.originalid_list` 一并改名、内部路径名统一；`epp_path`（tr_*）保留并 docstring 说明镜像关系。
4. **CSV `Out` 入 EXCLUDED_TYPES**（P2-5）：一行代码级修复。
5. **稳定长连接**：服务端减少会话中途重启（客户端侧断线不自动重连、不自动重拉 tools/list）。

---

## 六、全局运行时冒烟（2026-09-09 全量检查）

对 86 个工具做全量运行时冒烟（空参 `{}` + 按必需参数类型注入 dummy 实参；写类/启动类工具仅空参或 SKIP，避免副作用）：

- **结论：0 崩溃**——无 NameError/TypeError/AttributeError/KeyError/traceback/5xx，参数校验层干净。
- **定义完整性**：86 个工具 description **100% 覆盖**（无空/过短）；256 个参数 description **100% 覆盖**（0 缺失）。
- **代码安全**：仓库内无硬编码密钥；`.env` 已被 `.gitignore` 忽略；无英文 message 残留（message 已统一中文）。

---

## 七、冗余与精简建议（工具合并 / 优化路线）

### 明显冗余（建议合并）

| 现有 | 合并后 | 权衡 |
|---|---|---|
| `search_public_models` + `search_personal_models` | `search_models(sub_type, library=public/personal)` | 后端不同（公共模型服务 vs 个人库），可保留分开但 docstring 互指 |
| `search_schematic_from_public_library` + `search_schematic_from_personal_library` | `search_schematic_from_library(search_name, library=...)` | 同上 |
| `use_schematic_from_library_create_project` + `use_schematic_from_library_import` | `use_schematic_from_library(file_uuid, mode=create/import, project_path=?)` | 参数高度对称，合并价值最高 |

### 可精简（合并 / 三选一）

- **列器件四工具**（=P2-3）：`list_schematic_components`（gRPC 信封、无分页）/ `list_simulation_components`（扁平、offset/limit 分页）/ `tr_query_schematic_components`（TR 信封）/ `tr_query_components`（按型号查类别规格）。建议基础层保留一个统一返回 + 分页；TR 版镜像 SimulationAgent 契约可保留，但 docstring 点明「非 TR 流程用 list_simulation_components」。
- **`simulate_netlist` + `simulate_netlist_with_ads`**：一个走 EDI（默认 ADS 路径）、一个直连 ADS（显式 ads_path）。可合并为 `simulate_netlist(netlist_path, ads_path=None)`。

### 需优化（保留但改进）

- **异步查询命名三套不一致**：CST `*_query` / HFSS `get_*_status` / EDA `get_*_async_status`；且 `cst_export_snp` 无 query、`cst_export_farfield` 有 query（不对称）。建议统一命名约定 + 补齐 snP 查询。
- **搜索/模型库无分页**：`search_public_models`/`search_personal_models` 固定每页 10 条、无翻页参数，全量 283KB 撑爆上下文。应加 `offset`/`limit`（参照 `list_simulation_components`）。
- **`get_model_category_params` 全量 158KB**：`categories_only` 默认值建议反过来（默认精简、显式要全量）。

### TR 与基础工具重叠（不合并，需澄清定位）

`tr_query_schematic_components` / `tr_get_project_netlist` / `tr_run_simulation` 与 `list_schematic_components` / `export_project_netlist` / `simulate_netlist` 语义重叠，但 TR 是版本化可追溯工作流（网表修订不覆盖原网表），重叠是故意的。建议两组 docstring 各加一句「TR 版用于可追溯仿真流程，普通场景用基础版」。

### 落地优先级

1. `use_schematic_from_library_create/import` 合并（收益最高、成本最低）
2. `simulate_netlist` / `simulate_netlist_with_ads` 合并
3. 搜索工具统一 `offset/limit` 分页
4. 异步查询命名三套归一
5. 公共/个人库成对工具（权衡后端差异再决定）
6. 列器件四工具（配合 P2-3，文档层先点明定位）

---

## 八、代码级精简审查（过度工程审计，2026-09-10）

- **口径**：只审「过度工程 / 复杂度」——死代码、投机性分支、单实现抽象、无人设置的配置、只做转发的包装、手写标准库。**明确排除**正确性 bug、安全、性能（另案处理）。
- **方法**：3 个子代理并行扫 `servers/eda`+`turbocharts` / `ansys`+`cst`+`tr_simulation`+`multimodal_vision` / 顶层基础设施层，共 47 条原始发现；父代理逐条 `grep` 交叉复核后去重合并为 30 条（**修正 3 处子代理结论**，见本节末）。
- **分类标签**：`delete`（死代码/投机功能，替换物=无）／`stdlib`（手写标准库已有）／`native`（平台/框架已提供）／`yagni`（单实现抽象、无人设的配置、单调用方中间层）／`shrink`（同逻辑更短写法）。
- **基线**：审计基于工作树（当时有 10 个文件未提交，含 `eda/simulation.py`、`process_guard.py`、`tr_simulation/tools.py`）。

### 8.1 无风险死代码 / 死配置 —— 可直接删，≈95 行

| # | tag | 砍掉什么 | 证据（已复核） |
|---|---|---|---|
| A1 | delete | `listChanged` monkeypatch **34 行 + 24 行逐行注释**，只为让 `initialize` 带 `capabilities.tools.listChanged=true`；注释自认「当前工具 import 时静态注册、运行期不变，故未发送通知」——声明了可变却永不变更，且依赖 SDK 私有 `_mcp_server` | `servers/__init__.py:74-107`（`registry_server.py` 无运行期注册/注销逻辑，可交叉验证） |
| A2 | delete | 同步工具 `simulate_project`（已取消注册，零生产调用；同步仿真已被 `start_simulation_async` + `get_simulation_async_status` 覆盖） | 删 `servers/eda/simulation.py:431-458` + `eda/__init__.py:102` 导入 + `chat/service.py:742` 集合条目 |
| A3 | delete | **7 条死 `ERROR_CODES`**：`INVALID_PATH`/`FILE_NOT_FOUND`/`EDA_GRPC_OFFLINE`/`TOOL_TIMEOUT`/`TOOL_LOOP_LIMIT`/`SIMULATION_ALREADY_RUNNING`/`LLM_REQUEST_FAILED`。实测 `_tool_error` 只用到 4 条（DUPLICATE_TOOL_CALL / TOOL_EXECUTION_FAILED / MISSING_REQUIRED_ARGUMENT / NO_CURRENT_PROJECT） | `servers/chat/service.py:330-333,336-338` |
| A4 | delete | `Activity.args` 字段声明后从未赋值（唯一构造点 `:625` 不传它）；`ChatSession.simulation_task_ids` 只写不读（读它的只有测试） | `servers/chat/service.py:223,254,796-797` |
| A5 | delete | `_ChannelHandle` 上下文管理器：`__exit__` 仅 `pass`，把一句赋值包成 14 行类；全仓仅 1 处使用 | `servers/eda/grpc_client.py:93-106`（唯一使用点 `:311`） |
| A6 | delete | 第二个 `QUEUE_TIMEOUT` 分支与上一个逐字同构，注释自认「几乎不可达：acquire 成功意味着未超时」 | `servers/eda/grpc_client.py:250-260` |
| A7 | yagni | `mcp_host` 是**无法生效的配置**（拿到非 127.0.0.1 就 WARNING 强制改回，README 亦写明强制本地）；`openclaw_workspace` 全仓除定义外 0 处读取 | `servers/settings.py:48,82`；`start_servers.py:53,206-209` |
| A8 | delete | `CstApi._api_path` 只赋值不读；`get_setup_module` 的 `mod_name` 返回值两处调用都是 `module, _ =`；`eda/config.py` 的 `MCP_TRANSPORT` 常量无任何读取方（含一个多余三元判断） | `cst_api.py:117`；`ansys/config.py:137-144`；`eda/config.py:24` |
| A9 | delete | `_run_http_server(port, transport=...)` 的 `transport` 死参数（函数体全程不引用）；`_lifecycle_log` 纯转发包装（5 个调用点 3 处不传 extra） | `start_servers.py:197,318,80-82` |
| A10 | delete | **旧 token 鉴权残留**：`run.bat` 从 .env 读 `MCP_API_KEY` 拼 `?token=`。实测源码已无 token 中间件（`start_servers.py`/`chat/routes.py` grep 零命中），`.env` 里也没有 `MCP_API_KEY` → 该分支永不触发 | `scripts/run.bat:5-18`（`index.html` 同源 `AUTH_TOKEN` 逻辑一并清） |
| A11 | delete | `_workspace_note()` 只 `return "请使用资源管理器打开该文件。"`，模块 docstring 仍宣称「根据 OPENCLAW_WORKSPACE 返回自适应文案」（功能已删，注释过期） | `multimodal_vision/image_display.py:6,38-40` |
| A12 | delete | `_to_wire_parameters` 生产零调用（真实路径走 `_prepare_parameters`），仅 2 条单测调它 | `eda/simulation_components.py:311-320` |
| A13 | delete | **7 个 `.idea/` 文件已入库**，而 `.gitignore:6` 就写着 `.idea/`（gitignore 不会反删已跟踪文件） | `git rm -r --cached .idea` |
| A14 | delete | `docs/ACCESS_CONTROL.md` 被引用但**本目录下不存在**（文档早删、docstring 未跟上） | `servers/process_guard.py:18` |

### 8.2 小重构 —— 去重 / 换标准库，≈120 行

| # | tag | 现有 | 替换 | 证据 |
|---|---|---|---|---|
| B1 | shrink | `_terminal_result` **12 处调用**（238/252/345/358/451/460/474/486/503/516/529/539）每次重复同一批 kwargs（client_uuid/task_id/task_type_name/latest_details…），约 110 行里 60% 是样板 | 在 `_call_grpc_unlocked` 内定义 `finish(...)` 闭包填公共字段，各分支只传差异项，每处 9 行→1-2 行。**单点最大削减 ≈55 行**，纯等价改写；改后必跑 `test_bugfixes.py`/`test_simulation.py` 的 status/outcome_known/log_complete 断言 | `eda/grpc_client.py:238-547` |
| B2 | yagni | `com_session()` 上下文管理器**已存在**却仅 2 处使用，另有 **5 处手写** `pythoncom.CoInitialize()` + try/finally | 5 处改 `with com_session():`，删重复样板 ≈30 行 | `ansys/project_manage.py:34,120,283`；`ansys/run_analysis.py:37,84`；helper 在 `ansys/config.py:32` |
| B3 | stdlib | 手写 29 行 CSV 解析器（readlines/split/逐行 float/长度错位防护） | `csv.reader` ≈6 行，**保留「跳过坏行」语义**（勿用 `np.loadtxt`，遇坏行抛错会改行为） | `turbocharts/compare_results.py:220-248` |
| B4 | shrink | 两份几乎相同的四步路径校验（绝对路径→拒网络路径→is_file→扩展名白名单），而 `utils.validate_file`+`is_network_path` 已覆盖其中两步 | 各缩到 ≈4 行；`document.py` 版需保留「必须先绝对路径」这一步 | `multimodal_vision/validators.py:25-45`；`document.py:42-54` |
| B5 | shrink | 同一段「host:port 可达吗」手写 **4 遍、三种写法** | 抽一个 helper 四处调用 | `start_servers.py:139-146,212-233,249-252`；`chat/routes.py:29-41` |
| B6 | yagni | `_ensure_chat_tools()` + 锁 + **每次请求重建** `CHAT_TOOL_MAP`，但工具是 import 期静态注册、运行期不变（`:209` 已填好一次） | 删每请求调用与该锁 | `chat/service.py:195-209,478` |
| B7 | shrink | `resources_prompts/` 下 4 个文件（388 行）只是 `@mcp.prompt` + 静态 f-string，`__init__.py:38-63` 那 12 行 import 全为 `noqa: F401` 触发注册 | 合成 1 个模块，目录 7 文件→3（`test_mcp_content.py:93-154` 的导入需同步改） | `servers/resources_prompts/` |
| B8 | shrink | 测试桩 `_fake` **同一签名复制 6 份**、`_fake_caller` 2 份，仓库无 `conftest.py` | 挪进 `tests/conftest.py`，≈15 行 + 消除漂移 | `test_bugfixes.py:336,371,439,636`；`test_extended_ops.py:19`；`test_schematic_library.py:17` |
| B9 | shrink | 写完文件立刻再 `Path(...).exists()` 求证三次，`success` 就是 `img_ok` 却复合判断；`dep_key = dependency` 无意义别名 | 删冗余求证与别名 ≈6 行 | `turbocharts/compare_results.py:85-92,185,196,215` |
| B10 | shrink | `csv_non` 等 4 个中间变量恒等于入参 `csv_path`，`:414` 还重复判空一次 | 直接用 `csv_path` ≈5 行（输出路径与文件完全不变） | `turbocharts/convert_raw.py:393-402,414` |
| B11 | shrink | `_tool_args_summary`：25 行 + 11 条 per-tool 映射表，只为拼一行日志的 `args=` | 直接记 tool_name + 关键参数 | `chat/service.py:954-978`（唯一调用点 `:669-671`） |
| B12 | shrink | `list_eda_tasks` 先拷一份 keys 再逐 id 加锁深拷（同一把锁进出 N+1 次，首份 keys 拷贝多余） | 一次持锁内完成筛选+投影 | `eda/simulation.py:556-563` |

### 8.3 结构性 —— 削减最大，需拍板

| # | tag | 问题 | 建议 |
|---|---|---|---|
| C1 | yagni | **两套并行任务队列**：`TaskRunner` 的 docstring 自认「现有 EDA 仿真/HFSS 各自有一套，后续逐步迁移到本类」，但 `eda/simulation.py:48-96` 仍是完整一套平行实现（`_sim_tasks`+`_sim_lock`+`ThreadPoolExecutor(1)`+`_prune_tasks_locked`+`_TASK_TTL=7200`）。实测 HFSS 与 CST **共用** TaskRunner（`run_analysis.py:22`、`cst_api.py:24`），故是**两套**而非三套 | 二选一：eda 改用 `TaskRunner`，或删 `task_runner.py` 各留各的。不要两套并存 |
| C2 | yagni | **`tr_simulation/tools.py` 345 行 1:1 镜像上游 schema**：17 个 wrapper 每个只是 `call_tool(name, {参数再抄一遍})`，默认值逐字抄上游（文件 docstring 自认）。上游 schema 已是唯一真源，手工复制必然漂移 | 名义可省 ≈250 行（按 `/api/v1/integration/tools` 动态注册），**代价是丢掉手写的工具描述**（docstring 即客户端 tool description）。建议：先不动，或只让默认值从上游 schema 读取 |
| C3 | yagni | `ProcessProbeMiddleware` 是「阶段 1 调试探针」，却常驻启动路径（未配白名单时挂载，每条请求扫一次 psutil 全连接表）。`.env:42` 已配 `MCP_ALLOWED_PROCESSES`，生产走白名单分支，探针实际不走 | 删掉该中间件与 `else` 分支，或降为 debug 日志 |
| C4 | yagni | `signal_chain` 的 `direction` 参数：backward 只换起点（选 load），`_trace_chain` 未接 direction 入参，「向上游遍历」并未实现，最终又原样回显 | **半成品选项**——按 bug 修反向遍历，或按 yagni 删参数与回显。定性由作者决定 |

### 8.4 明确不动（信任边界，非过度工程）

`servers/report/generator.py` 的 16 步校验、`servers/chat/routes.py` 的上传大小限制、`_prepare_parameters` 及路径/枚举校验 —— 均为真实信任边界，审计口径已排除且经复核确认不应精简。

### 8.5 依赖结论

**无白装依赖，可减 0 个**：`pywin32` 一度疑似多余，复核后确认 `ansys/config.py:19-21` 真在用 `pythoncom` + `win32com.client.GetActiveObject` 驱动 AEDT；`psutil`/`numpy`/`matplotlib`/`python-dotenv` 各有明确使用点。全仓跨文件重名 def 仅 6 组、其中 5 组在 `tests/`——**未发现大规模复制粘贴**。

### 8.6 复核修正记录（子代理原始结论 → 实测）

1. ~~「TaskRunner / process_guard 重复同一件事」~~ → 不成立：前者是 Python 异步任务队列，后者是 HTTP 来源进程白名单（OS 层进程身份鉴权），职责无交集。真正的重复在 `task_runner.py` 与 `eda/simulation.py` 之间（C1）。
2. ~~「两套并行任务队列 = 三套」~~ → 实测为**两套**（TaskRunner 被 HFSS/CST 共用）。
3. ~~「`openclaw_workspace` 只是旧字段」~~ → 字段确为死配置（A7），但 `image_display.py:6` 是**过期注释**而非活配置，删字段时一并修注释。

### 8.7 落地优先级

1. **A 区 14 项一次性清掉**（零行为变更，一个提交；`.idea` 用 `git rm -r --cached`）
2. **B1 `_terminal_result` 闭包 + B2 `com_session` 复用**（收益最大的两处纯去重，改完跑 EDA/ansys 测试）
3. **B 区其余项**可增量做（B3 CSV→`csv`、B4 路径校验、B8 测试桩入 conftest 优先）
4. **C 区等作者拍板**（C1 二选一、C3 删探针、C4 定性；C2 建议先不动）

**net: 可减 ≈250-300 行 + 3 个文件（另 7 个 `.idea` 文件停止跟踪），可减依赖：0。**

---

## 九、2026-09-10 落地变更记录（评审基线已移动）

> 以下为本会话对仓库源码的两处改动。第八节过度工程审计的基线是改动前工作树，据此同步。

### 9.1 `success` 语义回归业务结果（`servers/eda/simulation.py`）

`get_simulation_async_result` / `get_simulation_async_status` 的 `success` 从「查询成功」改回「业务结果」：

| 状态 | success | task_success | outcome_known |
|---|---|---|---|
| RUNNING（进行中） | true | null | false |
| SUCCEEDED | true | true | true |
| FAILED | **false** | false | true |
| TIMEOUT / STREAM_DISCONNECTED | **false** | null | false |

权威细粒度信号仍为 `task_success` / `outcome_known`，未丢信息。配套更新 `docs/TOOLS_API.md`、`tests/test_simulation.py`（`TestResultSuccessSemantics` 5 条）、`tests/test_bugfixes.py`（1 条）。

### 9.2 进程白名单匹配重写（`servers/process_guard.py`）

原「裸子串匹配」（`a in haystack`，`"r"`/`"python"` 会误命中）改为三类精确规则：

- **通用解释器/Shell 裸名**（python、node、java、cmd、powershell 等）：`_GENERIC_RUNTIMES` 硬编码拒绝，防止误配放行任意进程。
- **路径型条目**（含 `/`）：exe 路径精确相等或目录前缀。
- **关键词型条目**（不含 `/`）：等于完整命令行 token（空白分隔）或 exe 文件名（basename）。

配套更新 `tests/test_process_guard.py`（8 条）。

### 9.3 对本报告的影响

- **P2-3**：`list_simulation_components` 的 `count` 字段已删（工作树改动），返回信封现为 `total/offset/limit/component_type_counts`。
- **第八节行号**：A2/A6/B1/B12/A14 等引用的 `simulation.py`、`process_guard.py` 行号因上述改动已漂移，重跑审计时刷新。
- **第六节行为抽样探针**：若按旧 `success` 语义下的结论，需重验。

---

## 附：实测原始返回片段（证据留档）

`batch_query_component`（未命中补 null + missing）：

```json
{"model_name_list": ["NC10355C_2931", "NO_SUCH_MODEL_XYZ"],
 "data": [{...命中项...}, null],
 "missing": ["NO_SUCH_MODEL_XYZ"]}
```

`tr_query_components`（已补 null + missing，与 batch_query_component 对齐）：

```json
{"success": true, "status": "completed",
 "result": {"success": true, "source": "edi_mms",
            "data": [{"model": "MAAD_008866", ...}, null],
            "missing": ["__NOT_EXIST_XYZ__"]},
 "operation_id": "op_..."}
```

`get_service_status`（channel_state 已修）：

```json
{"grpc_target":"127.0.0.1:50055","channel_state":"ready","channel_cached":true,"queue_locked":false,"max_receive_mb":256}
```

`export_schematic_components_to_csv`（CSV Out 漏导，save_path/project_path 已从 details 移除）：

```text
CSV 内容：
original_model_type,original_model_name,original_model_id,...
Out,Out1,,,,
NC10355C_2931,NC10355C_29311,36742914-1237-4530-baef-488e4765a920,,,

details 键（save_path/project_path 已移除）：
{"csv_path": "...check_out.csv"}
```

协议方法实测（P2-4 备忘）：

```text
prompts/list   OK  9 个: run_tr_simulation / inspect_edi_project / analyze_signal_chain /
                        run_and_review_simulation / assess_anti_burnout / configure_simulation_component /
                        select_component / create_simulation_report / troubleshoot_edi_error
resources/list OK  7 个: edi://integration/workflow / edi://service/overview / edi://service/status /
                        edi://projects / edi://reference/simulation-components /
                        edi://reference/operation-guide / edi://reference/error-codes
prompts/get run_tr_simulation  OK  (description + messages)
```
