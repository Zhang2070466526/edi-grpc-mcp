# EDI MCP 服务评审报告（待办问题清单）

- **评审日期**：2026-09-08 起持续更新，最新复审 **2026-09-10**
- **评审对象**：EDI MCP 服务（`http://127.0.0.1:50026/mcp`，`D:\GitLabCode\edi-grpc-mcp\start_servers.py`）86 个业务工具 + 仓库源码本身
- **评审方法**：① 运行形态侦察（netstat / 进程命令行 / `/ready` / `/health`）；② 全量 `tools/list` 定义审查（name/description/inputSchema）；③ 行为抽样探针（真实工具调用 + 白名单探针）；④ 读源码逐条 `grep` 复核；⑤ 代码级过度工程审计（3 子代理并行扫 + 父代理逐条复核）
- **说明**：本清单**只保留未解决问题**。已修复项按本文件既有规矩于 2026-09-10 三轮复扫后移除（留档：git `4a16cec` 及本文件删除前快照）。标签：`[契约]` 接口/命名/文档不一致；`[死代码]`/`[精简]`/`[结构]` 代码级过度工程；`[环境]` 本机运行环境。
- **当前运行形态（2026-09-10 16:37 复扫）**：**50026 未运行**（工作树改动未提交、未重启）；EDI.exe(50055, PID 28456)、SimulationAgent(17866/17867) 在跑。上一轮实测（16:11）：源码形态 uv py3.14 直跑 `start_servers.py`、`/ready` `tool_count=86` / `tools_hash=1e5c9305`、`/health` `ok`。

---

## 一、P0（高优先级）

### P0-1 🔐 SimulationAgent 密钥裸奔在进程命令行
- **现象**：`SimulationAgent.exe` 以 `--model-api-key sk-...` 与 `--edi-mms-api-token <JWT>` 作为 argv 明文启动。
- **影响**：本机任何进程/用户可 `ps`/WMI 读到明文密钥。
- **说明**：本仓库代码内无硬编码密钥、`.env` 已被 `.gitignore` 忽略——问题在 EDI 侧 `SimulationAgent.exe` 的启动参数，修复位置不在本仓库。
- **建议**：改从环境变量/配置文件读取；启动后清零 argv。

---

## 二、P1（契约 / 命名不一致）

| # | 问题 | 证据 / 说明 |
|---|---|---|
| P1-1 | 「型号/ID 列表」参数名不统一 | `batch_query_component.model_name_list`（仅型号串）／`tr_query_components.originalid_list`（UUID 或 model_name）／`get_components_static_params.original_uuids`（真 UUID）——三处仍不统一 |
| P1-2 | `originalid` 命名残留 | `tr_simulation/tools.py:319,322,329,333` 仍用 `originalid_list`（无下划线），建议对齐 `batch_query_component`（**2026-09-10 复核：仍未改**） |
| P1-3 | 工程路径多名字 | `list_epp_projects.folder_path` vs `create_project.path` vs 多数 `project_path` vs tr_* `epp_path`（镜像 SimulationAgent 契约，保留） |

---

## 三、P2（文档 / 质量）

- **P2-1　docstring 格式不统一**：多数「用法:」中文体，少数「Args:」英文体（`close_edi_project`、`capture_schematic`、`simulate_netlist_with_ads` 等）。
- **P2-2　返回字段键语言不一致**：`get_components_static_params` 中文键（封装/尺寸/成本/所属厂商/模型id/重量），`batch_query_component` 英文键（model/manufacturer/type）+ 中文 `specs` 串。
- **P2-3　列器件工具信封不一致（原「功能冗余」已收敛）**：`list_schematic_components` 是 gRPC 任务信封（task_id/status/details.component_count）、**无分页**；`list_simulation_components` 是扁平结构（`total/offset/limit/component_type_counts`，原 `count` 字段已删）、**带分页**；`tr_query_schematic_components` 又是 TR 信封。同一批器件多套信封、能翻页的不统一，调用方二选一很纠结。→ 基础层保留一个统一返回 + 分页，TR 版 docstring 点明「非 TR 流程用 `list_simulation_components`」。
- **P2-4　协议方法暴露为「伪工具」（备忘，非 bug）**：`get_prompt`/`list_prompts`/`list_resources`/`read_resource` 是 MCP 协议方法（非工具），服务端实测可用（`prompts/list` 9 个、`resources/list` 7 个、`prompts/get` 正常）。Hermes 会话快照把它们暴露成 4 个「伪工具」，经 `tool_call` 调会失败——纯客户端展示问题。
- **P2-5　CSV 导出漏 `Out` + docstring 指向不存在的常量**：
  - 实测 `export_schematic_components_to_csv`（工程 23）输出仍含 `Out,Out1,,,,` 一行；`TermG`/`SParameter`/`Sweep`/`Var`/`HarmonicBalance`/`XDB`/`P_nToneG` 均已正确排除 → 按 docstring「端口不导出」口径，`Out` 也应入排除表。**2026-09-10 16:11 当场复现**。
  - `servers/eda/design_export.py:90` 的 docstring 提到 `EXCLUDED_TYPES`，但**代码中无此标识符**（全仓 grep 仅命中该 docstring）——文档指向了一个不存在的东西，需一起订正。

---

## 四、代码级过度工程（未清理项，23 项）

**口径**：只审过度工程/复杂度——死代码、投机性分支、单实现抽象、无人设置的配置、只转发包装、手写标准库；**排除**正确性 bug、安全、性能。**边界（明确不动）**：`servers/report/generator.py` 的 16 步校验、`servers/chat/routes.py` 的上传大小限制、`_prepare_parameters` 及路径/枚举校验属真实信任边界。

### 4.1 死代码 / 死配置（10 项，可直接删）

| # | tag | 问题 | 证据（2026-09-10 复核） |
|---|---|---|---|
| A2 | 死代码 | `simulate_project` 同步工具残留，**引用点反而增至 5 处**：定义 + 导入 + chat 工具集 + 2 处 UI 映射 | `eda/simulation.py:432`、`eda/__init__.py:102`、`chat/service.py:725`、`chat/index.html:236,289` |
| A3 | 死代码 | 7 条死 `ERROR_CODES`（`INVALID_PATH`/`FILE_NOT_FOUND`/`EDA_GRPC_OFFLINE`/`TOOL_TIMEOUT`/`TOOL_LOOP_LIMIT`/`SIMULATION_ALREADY_RUNNING`/`LLM_REQUEST_FAILED`）——`_tool_error` 实际只用 4 条（DUPLICATE_TOOL_CALL / TOOL_EXECUTION_FAILED / MISSING_REQUIRED_ARGUMENT / NO_CURRENT_PROJECT） | `chat/service.py:314-322` |
| A4 | 死代码 | `Activity.args` 声明后从未赋值（唯一构造点 `:625` 不传它）；`ChatSession.simulation_task_ids` 只写不读（读它的只有测试） | `chat/service.py:207,238,779-780` |
| A5 | 死代码 | `_ChannelHandle` 上下文管理器 `__exit__` 仅 `pass`——把一句赋值包成 14 行类 | `eda/grpc_client.py:93-106`（唯一使用点 `:325`） |
| A6 | 死代码 | 第二个 `QUEUE_TIMEOUT` 分支与上一个逐字同构，注释自认「几乎不可达」 | `eda/grpc_client.py:250-260` |
| A8 | 死代码 | `CstApi._api_path` 只赋值不读；`get_setup_module` 的 `mod_name` 返回值两处调用都 `module, _ =`；`eda/config.py` 的 `MCP_TRANSPORT` 无任何读取方 | `cst/cst_api.py:117`；`ansys/config.py:139-141`；`eda/config.py:24` |
| A9 | 死代码 | `_run_http_server(port, transport=...)` 的 `transport` 死参数；`_lifecycle_log` 纯转发包装（5 个调用点 3 处不传 extra） | `start_servers.py:196,318`；`:79-81`（5 处调用） |
| A11 | 死代码 | `_workspace_note()` 只是返回常量的函数（`return "请使用资源管理器打开该文件。"`）——**过期 docstring 行已清，函数本体与调用点仍在** | `multimodal_vision/image_display.py:37-39,53` |
| A12 | 死代码 | `_to_wire_parameters` 生产零调用（真实路径走 `_prepare_parameters`），仅 2 条单测调它 | `eda/simulation_components.py:311-320` |
| A14 | 死引用 | docstring 引用 `docs/ACCESS_CONTROL.md`，**该文件不存在** | `servers/process_guard.py:18` |

> 已清（留档 git `4a16cec` + 2026-09-10 工作树改动）：`listChanged` monkeypatch 34 行 + 24 行注释、`run.bat`/`index.html` 旧 token 鉴权、7 个 `.idea/` 文件停止跟踪、`_terminal_result` 12 处样板收敛 `finish()` 闭包、**A7 死配置两项**（`mcp_host`/`openclaw_workspace`，含 README 与 image_display 过期注释）、**B2 `com_session` 迁移**（ansys 5 处手写 CoInitialize + try/finally → `with com_session():`）、**B6 每请求重建工具表**（`_ensure_chat_tools` + 锁已删）。

### 4.2 去重 / 换标准库（9 项）

| # | tag | 现有 | 替换 | 证据 |
|---|---|---|---|---|
| B3 | stdlib | 手写 29 行 CSV 解析器（readlines/split/逐行 float/长度错位防护） | `csv.reader` ≈6 行，**保留「跳过坏行」语义**（勿用 `np.loadtxt`，遇坏行抛错会改行为） | `turbocharts/compare_results.py:220-248` |
| B4 | 精简 | 两份几乎相同的四步路径校验（绝对路径→拒网络路径→is_file→扩展名白名单），而 `utils.validate_file`+`is_network_path` 已覆盖其中两步 | 各缩到 ≈4 行；`document.py` 版需保留「必须先绝对路径」 | `multimodal_vision/validators.py:25-45`；`document.py:42-54` |
| B5 | 精简 | 同一段「host:port 可达吗」手写 **4 遍、三种写法** | 抽一个 helper 四处调用（`edi_launcher.py` 已有 `channel_ready_future` 版可复用） | `start_servers.py:142,215,229`；`chat/routes.py:34`；`eda/edi_launcher.py:63,101` |
| B7 | 精简 | `resources_prompts/` 下 4 个文件（388 行）只是 `@mcp.prompt` + 静态 f-string，`__init__.py:38-63` 的 import 全为 `noqa: F401` 触发注册 | 合成 1 个模块（7 文件→3）；`test_mcp_content.py:93-154` 导入需同步改 | `servers/resources_prompts/` |
| B8 | 精简 | 测试桩 `_fake` **同一签名复制 9 份**（`def _fake` 全仓 9 处）、`_fake_caller` 2 份，仓库无 `conftest.py` | 挪进 `tests/conftest.py`，≈15 行 + 消除漂移 | `test_bugfixes.py:336,371,439,636`；`test_extended_ops.py:19`；`test_schematic_library.py:17,95` |
| B9 | 精简 | 写完文件立刻再 `Path(...).exists()` 求证三次；`success` 就是 `img_ok` 却复合判断；`dep_key = dependency` 无意义别名 | ≈6 行 | `turbocharts/compare_results.py:92,185,196,199,205,213,215` |
| B10 | 精简 | `csv_non` 等 4 个中间变量恒等于入参 `csv_path`，`:414` 还重复判空 | 直接用 `csv_path` ≈5 行（输出不变） | `turbocharts/convert_raw.py:393-402,414` |
| B11 | 精简 | `_tool_args_summary`：25 行 + 11 条 per-tool 映射表，只为拼一行日志的 `args=` | 直接记 tool_name + 关键参数 | `chat/service.py:937-961`（调用点 `:652`） |
| B12 | 精简 | `list_eda_tasks` 先拷 keys 再逐 id 加锁深拷（同一把锁进出 N+1 次） | 一次持锁内完成筛选+投影 | `eda/simulation.py:128,562` |

### 4.3 结构性（4 项，需拍板）

| # | tag | 问题 | 建议 |
|---|---|---|---|
| C1 | 结构 | **两套并行任务队列**：`TaskRunner` 的 docstring 自认「后续逐步迁移到本类」，但 `eda/simulation.py:48-79` 仍是完整一套平行实现（`_sim_tasks`+`_sim_lock`+`ThreadPoolExecutor(1)`+`_prune_tasks_locked`+`_TASK_TTL=7200`）。实测 HFSS/CST 共用 `TaskRunner`，故是**两套** | 二选一：eda 改用 `TaskRunner`，或删 `task_runner.py` 各留各的。不要两套并存 |
| C2 | 结构 | **`tr_simulation/tools.py` 345 行 1:1 镜像上游 schema**：17 个 wrapper 每个只是 `call_tool(name, {参数再抄一遍})`，默认值逐字抄上游（docstring 自认）。上游 schema 已是唯一真源，手工复制必然漂移 | 名义可省 ≈250 行（按 `/api/v1/integration/tools` 动态注册），**代价是丢掉手写工具描述**（docstring 即客户端 tool description）→ 建议先不动，或只让默认值从上游 schema 读 |
| C3 | yagni | `ProcessProbeMiddleware` 是「阶段 1 调试探针」，却常驻启动路径（未配白名单时挂载，每条请求扫一次 psutil 全连接表）；`.env:42` 已配白名单，生产不走它 | 删该中间件与 `else` 分支，或降为 debug 日志。`process_guard.py:124`；`start_servers.py:281` |
| C4 | yagni | `signal_chain` 的 `direction` 参数：backward 只换起点（选 load），`_trace_chain` 未接 direction 入参，「向上游遍历」并未实现，最终又原样回显 | **半成品选项**——按 bug 修反向遍历，或按 yagni 删参数与回显。`eda/signal_chain.py:187,214-216,234-236` |

---

## 五、本轮复核实测新增（未解决）

| # | tag | 问题 | 证据 |
|---|---|---|---|
| N1 | 死代码 | 白名单放行分支里残留**两行注释掉的调试日志**（`# _log.info(...)` / `# print(...)`） | `servers/process_guard.py:175-176` |
| N2 | 精简 | MCP 层替上游补契约：`tr_query_components` 客户端重排 + 补 null + 加 `missing`（≈10 行），条件 `not data or any(...)` 可读性差 | `tr_simulation/tools.py:335-344`。真修复宜在上游（SimulationAgent 按输入顺序对齐）；保留则建议加 `ponytail:` 注释标注上限 |
| N3 | 工具坑 | **「探针脚本文件名含 `hermes_cli`」取数法已失效**：白名单改精确匹配（整词 token / exe basename / 路径前缀，解释器裸名硬拒）后该脚本实测 **403**。可用替代：`python -m hermes_cli.main mcp test eda`（整词 token 命中；显示会截断但计数可信）。技能 `scripts/hermes_cli_probe_tools.py` 需改用法 | `.env:42` = `MCP_ALLOWED_PROCESSES=hermes_cli.main,edi-agent-service.exe`；`start_servers.py` 白名单挂载段 |
| N4 | 环境 | **EDI GUI 的 ADS 路径需确认**：`C:\Program Files (x86)\EDI\config\config.ini` 的 `[ads] hpeesof_dir` 曾指向不存在的 `ADS2020`（本机实为 `ADS2020_update2`），导致仿真 0.2 s 即失败、报 `Can not find …/ADS2020`（易误判为 MCP 工具故障）。已改文件并验证通过，但该文件 **EDI 启动时会重写**（mtime = 启动时间）→ 需在 GUI 设置里同样指到 `ADS2020_update2`，否则会复现 | `config.ini.bak-20260910` 为改动前备份 |

---

## 六、工具合并 / 精简路线（服务端接口层）

### 明显冗余（建议合并）

| 现有 | 合并后 | 权衡 |
|---|---|---|
| `search_public_models` + `search_personal_models` | `search_models(sub_type, library=public/personal)` | 后端不同（公共模型服务 vs 个人库），可保留分开但 docstring 互指 |
| `search_schematic_from_public_library` + `search_schematic_from_personal_library` | `search_schematic_from_library(search_name, library=...)` | 同上 |
| `use_schematic_from_library_create_project` + `use_schematic_from_library_import` | `use_schematic_from_library(file_uuid, mode=create/import, project_path=?)` | 参数高度对称，合并价值最高 |

### 可精简

- **列器件多工具**（=P2-3）：信封与分页不统一，先统一返回契约，TR 版 docstring 点明定位。
- **`simulate_netlist` + `simulate_netlist_with_ads`**：一个走 EDI（默认 ADS 路径）、一个直连 ADS（显式 `ads_path`）→ 可合并为 `simulate_netlist(netlist_path, ads_path=None)`。

### 需优化（保留但改进）

- **异步查询命名三套不一致**：CST `*_query` / HFSS `get_*_status` / EDA `get_*_async_status`；且 `cst_export_snp` 无 query、`cst_export_farfield` 有 query（不对称）。
- **搜索/模型库无分页**：`search_public_models`/`search_personal_models` 固定每页 10 条、无翻页参数，全量 283KB 撑爆上下文 → 应加 `offset`/`limit`（参照 `list_simulation_components`）。
- **`get_model_category_params` 全量 158KB**：`categories_only` 默认值建议反过来（默认精简、显式要全量）。

### TR 与基础工具重叠（不合并，需澄清定位）

`tr_query_schematic_components` / `tr_get_project_netlist` / `tr_run_simulation` 与 `list_schematic_components` / `export_project_netlist` / `simulate_netlist` 语义重叠，但 TR 是版本化可追溯工作流（网表修订不覆盖原网表），重叠是故意的 → 两组 docstring 各加一句「TR 版用于可追溯仿真流程，普通场景用基础版」。

---

## 七、落地优先级

1. **4.1 死代码 10 项一次性清掉**（零行为变更，一个提交；A2 引用点已增至 6 处，先清）
2. **B3 CSV→`csv` + B5 TCP 探活去重**（当前收益最大的两处去重；B2 已完成）
3. **P2-5 CSV `Out` 入排除表**（一行级，已复现，改完用 `export_schematic_components_to_csv` 复验）+ N1/N2 顺手清
4. **工具合并**：`use_schematic_from_library_create/import`（收益最高、成本最低）→ `simulate_netlist` 系列 → 搜索工具统一分页
5. **C 区拍板**（C1 二选一、C3 删探针、C4 定性；C2 建议先不动）
6. **P0-1 密钥治理**（EDI 侧修复）+ **N4 确认 EDI GUI 的 ADS 路径**

---

## 附：实测原始返回片段（证据留档）

`batch_query_component`（未命中补 null + missing）：

```json
{"model_name_list": ["NC10355C_2931", "NO_SUCH_MODEL_XYZ"],
 "data": [{...命中项...}, null],
 "missing": ["NO_SUCH_MODEL_XYZ"]}
```

`tr_query_components`（已补 null + missing，与 batch_query_component 对齐）：

```json
{"success": true, "status": "completed",
 "result": {"success": true, "source": "edi_mms",
            "data": [{"model": "MAAD_008866", ...}, null],
            "missing": ["__NOT_EXIST_XYZ__"]},
 "operation_id": "op_..."}
```

`export_schematic_components_to_csv`（P2-5：`Out` 仍漏导，工程 23，2026-09-10 复现）：

```text
original_model_type,original_model_name,original_model_id,alternative_model_type,alternative_model_name,alternative_model_id
Out,Out1,,,,
NC10355C_2931,NC10355C_29311,36742914-1237-4530-baef-488e4765a920,,,
```

协议方法实测（P2-4 备忘）：

```text
prompts/list   OK  9 个: run_tr_simulation / inspect_edi_project / analyze_signal_chain /
                        run_and_review_simulation / assess_anti_burnout / configure_simulation_component /
                        select_component / create_simulation_report / troubleshoot_edi_error
resources/list OK  7 个: edi://integration/workflow / edi://service/overview / edi://service/status /
                        edi://projects / edi://reference/simulation-components /
                        edi://reference/operation-guide / edi://reference/error-codes
prompts/get run_tr_simulation  OK  (description + messages)
```
