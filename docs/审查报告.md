# EDI MCP 服务审查报告（未解决问题清单）

- **最后更新**：2026-09-20（逐条对**运行中的服务**与**工作树代码**坐实；已修项不在此登记）
- **扫描范围口径**：`.py` 98 个（`servers/`+`tests/`+`scripts/`+`start_servers.py`）、全仓文件 180 个（排除 `.venv`/`build`/`dist`/`__pycache__`）、18,890 行
- **对象**：EDI MCP 服务（`http://127.0.0.1:50026/mcp`）91 个业务工具 + 仓库源码
- **方法**：① 运行形态侦察；② 全量 `tools/list` 审查；③ 行为抽样探针（真调 + 白名单 + 进程内直调）；④ 读源码 `grep` 复核；⑤ 子代理并行静态扫全仓 + 父代理汇总
- **标签**：`[崩溃]` 穿透 MCP 边界的异常；`[死代码]`/`[精简]`/`[结构]` 过度工程；`[契约]` 接口/命名/文档不一致；`[环境]` 本机运行环境；`[依赖侧]` 本仓库改不了、仅跟踪
- **口径**：本报告**只记未解决问题 + 修复建议**；已修项一律不登记（历史见 git log）。判「死代码」须区分**死代码路径**（函数/字段/分支，删）与**词典/公共 API/防御冗余**（查表完整性/对外契约/安全冗余，留）
- **交叉参考**：TR 集成实质问题（会话归属串台、确认门空壳、17 工具认知负担）→ `servers/tr_simulation/TR集成问题清单.md`，本报告只收其文档漂移类条目
- **运行态**：50026 运行中（源码方式、`--host 0.0.0.0`；`bind_host=0.0.0.0`、`allowed_hosts=16`、`tool_count=91`、`tools_hash=fff550e2`）。进程启动 **17:41:57**，已含 `fetch_artifact`（第 91 个工具）与 offload/互斥锁；**早于**工作树最新的报告链接口径改动（`report/generator.py` + `utils.py`）⇒ 那部分**重启后才生效**。**`allowed_hosts` 由 18 降为 16 不是缺陷**：VPN 网卡（`10.111.222.1`）已下线，被动态枚举自动移出；现网卡只剩「以太网」`192.168.0.58` + 非环回 IPv6。**EDI 未运行**（`50055` 无监听）→ `grpc=offline`、`/health=degraded`，此刻所有 EDA/ADS 工具必失败。今日日志 **1 处 ERROR**，为 13:34:39 的一次 Proactor 断连噪音（`R49`），16:29 重启后 0 处。


## 一、总览（优先级阶梯）

> **一句话**：可用性层面的崩溃/异常逃逸已清零，**当前没有「agent 必然踩且无法自愈」的问题**。09-21 那批已把「出事之后说不清」的**主体补齐并实测生效**（任务日志、守卫/探针落日志、启动自检、预检 0.5s→0.15s）；剩下的三类：① **漏检与顺序**（`R46-残` 自检漏检 CST + `Popen` 未绑句柄；`R54` 日志顺序/被拒无痕）；② **契约/精简小改**（`R23`/`N1`/`P2-5`/`P2-8`/`P2-9`/`P2-10`）；③ **加固待办**（token 链接/`fetch_artifact` 无「允许根目录」；`R42` 已按你的决定暂缓）。

**ROI 阶梯**

| 梯队 | 做什么 | 为什么排这里 |
|---|---|---|
| **P0.5-可追溯（收尾）** | **`R46-残`**：启动自检的进程名过滤器漏检 CST（**实测** 3 个 CST 进程常驻却零 WARN）+ 拉软件的 `Popen` 未绑 kill/terminate/atexit；**`R54`**：`task_runner` 新日志的 QUEUED 顺序颠倒 / 被拒无痕 / FAILED 用 INFO —— 修法 **§13.1** | 日志主体已可用；这两条是「漏检」与「可读性」，不阻塞远程部署 |
| **P1-加固（可选，你定）** | **token 链接 / `fetch_artifact` 无「允许根目录」限制** —— 引擎机任意可读文件都可被取（实测 `C:/Windows/win.ini` 可取）；本期「不做鉴权」下这是唯一放大面。修法 ≈10 行（默认工作区，留空=任意）。**`R42`（接入控制）已按你的决定暂缓**，见 **§13.3** | 远程可用性本身已闭环；这条是**安全加固**而非功能缺失 |
| **P1-零风险小改** | `R23`（恒真变量）/`N1`（2 行注释残留）/`register_document_url` docstring——一行到十行级，逐条修法 **§13.5** | 顺手、无行为变更 |
| **P2-契约对齐** | `T5`（先统一失败信封）、`T3-尾`（两处拍板）、`P2-5`（`Out`/`EXCLUDED_TYPES`）、`P2-8`（CST 导出不给链接）、`P2-9`（文档漂移 4 处）、`P2-10`（缺参回 SDK 裸校验文本）、`P1-*` 参数命名、`S1` 别名过渡 —— 倾向见 **§八** | agent 每次调用都经过这一层，低风险局部改动 |
| **P3-测试盲区** | **端到端 smoke**：全仓没有任何测试真起 MCP app、真调一次工具（**方案已跑通，见 §9.2**）；**＋ `generate_simulation_report` 成功路径零覆盖**（只测失败分支，见 §四） | 分类已能分辨真跑/跳过，但「服务真起来后能不能调通」仍无自动化覆盖 |
| **P4-结构性** | `C1`（两套并行任务队列）二选一、`C2`（TR wrapper 加漂移校验） | 动主链路需回归，收益是维护者侧 |
| **依赖侧**（本仓改不了） | `P0-1`：`SimulationAgent.exe` 以 `--model-api-key sk-...` 明文 argv 启动（§十一） | 修复在 EDI 侧；本仓已确认无硬编码密钥、`.env` 已 gitignore |
| **观察项 / 已知代价** | `R4-残`（UTF-8 无 BOM 静默错字，已接受）、`R11`/`R14`（需 AEDT 环境或接口改动才能定论）、`R47`（**已复现**：缺参 → 打整段 ERROR 堆栈 + 回 `code:0`）/`R49`（断连噪音）、`P2-6`/`P2-7`（软 IP 带宽能力缺口） | 见 §二 / §七 / §13.6 |
| **不做** | ① 为「数量好看」合并工具（90→88 够）；② 删「词典/公共 API/防御冗余」；③ 动真实信任边界（`report/generator` 校验链、`chat/routes` 上传限制、`_prepare_parameters` 校验） | 见 §九 明确不动项 |


## 二、崩溃 / 错误处理（本仓库侧，会穿透 MCP 边界）

> 共性：信任边界处对 `None`/非 str/异常文件状态无守卫，抛原生异常而非 `error_response`。本节只列**未修**项。

| # | 位置 | 问题 |
|---|---|---|
| **R4-残（残留·已接受）** | `utils.py:decode_local_text` 的三个调用点 | 当前实现 **BOM → cp936 优先 → utf-8 兜底**（`servers/utils.py::decode_local_text`；实测 GBK/ASCII/UTF-8 带 BOM 正确、二进制不崩）。**已知代价（已接受）**：⚠️ **UTF-8 无 BOM** `'专业'` → `'涓撲笟'`（静默错字）；注意**并非所有** UTF-8 内容都会错——`'中文节点'` 的 UTF-8 无 BOM 反而正确（其字节在 cp936 下非法 → 自动回退 utf-8），**错字与否取决于具体字节、不可预测**。**现实风险低**：三个读点全是 EDI 生成本机文件 + Qt 控制台（中文 Windows → ANSI），全仓仅一处写 UTF-8（`compare_results.py:197` 的对比 CSV，不会被这三处读回）。**唯一现实触发场景**：有人用编辑器（如 Win10 记事本）把工程文件另存为 UTF-8 **无 BOM** → 静默错字 |
| R11 | `ansys/config.py:105` | `_attach_aedt` 兜底 ProgID 返回 desktop 对象本身，`GetAppDesktop()` 必抛，「多 ProgID 兜底」分支走不通 —— 🔸 **已降为观察项**（复核：`_COM_PROGIDS` 两项都会 `try`，失败抛的是文档化的 `RuntimeError`；「兜底是否真走不通」需 AEDT 环境才能定论，不在本仓库侧判定） |
| R14 | `ansys/run_analysis.py:166` | 用 `pending_count()>0` 区分 `ANALYSIS_BUSY`/`TASK_LIMIT_REACHED` 不可靠 |
| R47 | `mcp 1.28` prompt 渲染（`fastmcp/server.py:1075` → `prompts/base.py:149`）| **`prompts/get` 少传必填参数 → SDK 抛 `ValueError` 并打整段 ERROR 堆栈**（不是统一错误码）。**✅ 已在当前版本复现（2026-09-21 09:18 真调**：`prompts/get{name:"inspect_edi_project"}` 缺 `project_path`）→ 客户端侧回 `{"error":{"code":0,"message":"Missing required arguments: {'project_path'}"}}`，**服务端日志照打 1 个 ERROR + 完整堆栈**（`prompts/base.py:149 raise ValueError`）。**两个问题点**：① 日志噪音（与 `R49` 同类）；② **JSON-RPC 错误对象里 `code: 0`**（0 在 JSON-RPC 里表示无错误）⇒ 只按 `code == 0` 判成功的客户端会误判。**SDK 层行为，本仓拦不住**（除非在 `prompts/get` 前置校验并自造错误响应）|
| R49 | Windows `asyncio` Proactor 事件循环 | **客户端异常断开 → 打整段 `ERROR Exception in callback _ProactorBasePipeTransport._call_connection_lost()` + 堆栈**（`ConnectionResetError [WinError 10054]`）。**复核（2026-09-20）**：全日志 **4 次**（09-02 23:33、09-07 23:23、09-14 09:44、09-20 08:40，均为客户端猛断连接）；**13:34 重启后的当前服务 0 次**。无害但污染日志 → 🔸 **观察项**（可选 `loop.set_exception_handler` 吞掉，≈8 行，**全局副作用须实测**）|

---

## 三、资源 / 安全

| # | 位置 | 问题 |
|---|---|---|
| R46-残 | `start_servers.py`（自检）+ `ansys/project_manage.py:154/349` | **任务日志与守卫日志已补**（2026-09-21 10:11 重启后**实测**：`task=… status=QUEUED/RUNNING/SUCCEEDED/FAILED`、`GUARD mode=…`、`process-probe …`、`启动自检：…` 均已落日志）。**剩余两处**：① **自检进程名过滤器漏检 CST** —— 只匹配 `ansysedt.exe` 与 `cst design environment_amd64.exe`，而**实测**本机在跑的 CST 是 `cstd.exe`(pid 5420)、`CSTDCMainController_AMD64.exe`(4048)、`CSTDCSolverServer_AMD64.exe`(4056)，**三者自 09-04 17:52:25 起常驻**，10:11:08 那次自检**一条 WARN 都没打**（静默漏检）；且仍不扫 `*.aedt.lock`；② 拉起软件的 `subprocess.Popen`（`ansys/project_manage.py:154/349`）**仍未绑** kill/terminate/atexit → 服务被杀时 AEDT/CST 继续跑、占着软件与锁 |
| R54 | `servers/task_runner.py:96-101` | **新增日志的几处瑕疵**（2026-09-21 真跑实测）：① `_log.info("…QUEUED…")` 写在 `self._executor.submit(...)` **之后** ⇒ 快任务**日志顺序颠倒**（实测：`status=RUNNING` 先于 `status=QUEUED op=probe_ok`）；② `submit` 返回 `None` 的两条路径（`require_idle` 忙 / 达 `max_tasks`）**无日志** ⇒「任务没起来」查不到原因；③ `FAILED` 用 `INFO` 且无 traceback；④ `except Exception: pass` 会把「psutil 缺失」吞掉（自检静默失效）|
| **R53** 🔸 | `mcp 1.28` 的 `server/streamable_http.py:494`（`json.loads(body)`）| **非法 UTF-8 请求体 → `-32603 Error handling POST request` + 整段 ERROR 堆栈，一条请求打 2 个 ERROR 块**（`Error handling POST request` + `Received exception from stream`）。实测（2026-09-20 17:43，我用 `curl` 把中文路径经 MSYS 转成 GBK 发出所致）：`UnicodeDecodeError: 'utf-8' codec can't decode byte 0xcf`。**SDK 层、本仓拦不住**（真 MCP 客户端不会发非 UTF-8），但会污染日志（与 `R47`/`R49` 同类）；要干净只能在自定义 400 处理里包一层 |
| R20 | `tr_simulation/client.py:52-54` | `requires_confirmation` 工具恒注入 `confirmed_by_user:True`，确认门空转（实质问题已交叉登记 `TR集成问题清单.md`，此处仅记位置） |
| R42 🔐 | `process_guard.py:140` `_PROTECTED_PREFIXES = ("/mcp",)` | **接入控制只覆盖 `/mcp`**：实测带非环回 Host 可直接访问 `/tools/list`（**200，吐全量工具名+描述**）、`/metrics`（200）、`/ui`（200）、`POST /upload`（400「no file」→ 端点可达）、`POST /chat`（400「message required」→ 可达）。且白名单留空时挂的是 `ProcessProbeMiddleware`（**每条请求 `psutil.net_connections()` 全表扫描 + print**）。远程模式自动忽略白名单（`--host` 非环回，第十九轮已落地，无需再手动留空 `.env`）。**2026-09-20 实测**：远程模式（`.env` 里白名单**仍配着**）下，一个**不在白名单**的进程 `POST /mcp` initialize（`Host` = `127.0.0.1:50026` 与 `192.168.0.58:50026`）→ 均 **200**；`GET /tools/list` → **200**（7194 B，吐全量工具名+描述）；`GET /ready` 经 `192.168.0.58` 与 `10.111.222.1` → 均 **200** ⇒ 坐实「远程模式 = 白名单被忽略 = 无接入控制」→ **等于无接入控制**，且已无 token 机制可用（`MCP_API_KEY` **代码域**零命中，见 §13.3）。🔸 **本期决策（2026-09-18）：暂不修** —— 用户选择「直接访问、内网可信」，远程＝无鉴权开放；`TokenGuard` 中间件原型已备好（§13.3，11/11），后续 ~25 行可一步加上 |
| P0-1 🔐 | EDI 侧 | `SimulationAgent.exe` 以 `--model-api-key sk-...` / `--edi-mms-api-token <JWT>` 明文 argv 启动（2026-09-15 复现），本机任何进程可读。**依赖侧**：修复在 EDI 侧（改环境变量/配置文件读取 + 启动后清零 argv），本仓库无硬编码密钥、`.env` 已 gitignore |

---

## 四、死代码 / 无效测试

| # | 位置 | 问题 |
|---|---|---|
| `register_document_url` | `multimodal_vision/document.py:69` | 报告改用 `build_file_link(disposition=…)` 后，**生产代码里已无调用点**（仅 `tests/test_bugfixes.py` 2 条用例 + `multimodal_vision/__init__.py` 的 re-export）⇒ 按「公共 API 保留不删」**保留**，但 `document.py:4` 的「供 report 等模块注册文档 Token 并返回预览链接」已过期，措辞需订正（1 行）；另注意它只放行 10 种办公格式（`_ALLOWED_EXTENSIONS`），与 `_register_any_file_url`（不校验）语义不同 |
| `generate_simulation_report` 成功路径 | `tests/test_report_generator.py` | 只覆盖 16 步校验的**失败分支**，成功分支（`report/generator.py:393-420`：token 链接三处一致 + PDF/DOCX 的 `disposition`）**零测试覆盖** ⇒ 今天这条改动无法用测试锁住。建议补 1 条 mock 渲染服务的单测（断言 `preview_url == download_url == markdown_link` 里的 URL、DOCX 走 `attachment`）|
| N1 | `process_guard.py:173-174` | 白名单放行分支残留两行注释掉的调试日志（`# _log.info(...)` / `# print(...)`） |

---

## 五、精简 / 一致性

| # | tag | 位置 | 问题 |
|---|---|---|---|
| B7 | 精简 | `resources_prompts/` 7 文件 663 行 | 多为 `@mcp.prompt` + 静态 f-string，可合成 1 模块（7→3）；`test_mcp_content.py` 导入同步改。⚠️ §九 定为**不改**（按语义拆可读） |
| 行尾混用 | 一致性 | `servers/` 内 | `utils.py`/`__init__.py`/`model_library.py`/`task_runner.py` 等为 **CRLF**，而 `report/generator.py`/`multimodal_vision/document.py`/`token_registry.py`/`turbocharts/convert_raw.py` 为 **LF** ⇒ git（`core.autocrlf`）下次 checkout 会把 LF 那批**整篇重写**，产生一次无意义的全文件 diff；建议用 `.gitattributes` 固定或一次性归一（同时消除每次 `git diff` 的 “LF will be replaced by CRLF” 警告）；**09-21 这批改动新增的 LF 文件**：`build.ps1`、`chat/service.py`、`eda/soft_ip.py`、`document.py`、`process_guard.py`、`task_runner.py`、`tr_simulation/tools.py`、`turbocharts/compare_results.py` —— `git diff` 已警告「LF will be replaced by CRLF」，**建议加一行 `.gitattributes`**（`* text=auto`，或 `*.py text eol=crlf`）一次定死；否则下次 checkout 会整篇重写这些文件（diff 噪音 / blame 全变）|

---

## 六、结构性（需拍板）

| # | 问题 | 建议 |
|---|---|---|
| C1 | **两套并行任务队列**：`TaskRunner` 的 docstring 自认「后续逐步迁移到本类」，但 `eda/simulation.py:48-79` 仍是完整一套平行实现（`_sim_tasks`+`_sim_lock`+`_SIM_EXECUTOR`+`_prune_tasks_locked`+`_TASK_TTL`）；实测 HFSS/CST 共用 `TaskRunner` | 二选一：eda 改用 `TaskRunner`，或删 `task_runner.py` 各留各的。⚠️ §九 定为**现阶段不合并**（要先给 `TaskRunner` 加进度回调、动仿真主链路，风险 > 收益） |
| C2 | `tr_simulation/tools.py`（345 行，17 个 `@mcp.tool()`）1:1 镜像上游 schema，默认值逐字抄上游（与 `MCP_WORKFLOW.md` 各存一份） | **不建议删 wrapper**（docstring 即 tool description）。改为加校验：启动/CI 拉 `/api/v1/integration/tools` 比对参数名与默认值，漂移即失败 |

> **已定性保留（不再重复上报）**：**C3** `ProcessProbeMiddleware` 调试探针（排查客户端 403/连不上反查来源进程）；**C5** `eda/__init__.py` 的 56 个 re-export（`README.md:119` 文档化公开 Python API）；**A16** `TaskRunner.count()/prune()`（通用类公共 API）；**A3** `ERROR_CODES` 中 `_tool_error` 未直接用到的几条（错误消息词典，查表完整性）。

---

## 七、契约 / 命名 / 文档

| # | 问题 | 证据 / 说明 |
|---|---|---|
| P1-1 | 「型号/ID 列表」参数名不统一（**2026-09-21 真调新增一例**） | `batch_query_component.model_name_list`（仅型号串）／`tr_query_components.originalid_list`（UUID 或 model_name）／`get_components_static_params.original_uuids`（真 UUID）三处仍不统一；**同类新例（2026-09-21 真调）**：`search_public_models` 要 `sub_type`，而 `get_model_category_params` 返回的字段写作「子类id」⇒ 按「子类id」猜参数名会直接吃 pydantic 校验错（详见 `P2-10`）|
| P1-2 | `originalid` 命名残留 | `tr_simulation/tools.py` 用 `originalid_list`（无下划线），建议对齐 `batch_query_component` |
| P1-3 | 工程路径多名字 | `list_epp_projects.folder_path` vs `create_project.path` vs 多数 `project_path` vs tr_* `epp_path`（镜像 SimulationAgent 契约，保留） |
| P2-1 | docstring 格式不统一 | 多数「用法:」中文体，少数「Args:」英文体（`close_edi_project`、`capture_schematic`、`simulate_netlist_with_ads` 等） |
| P2-2 | 返回字段键语言不一致（直调复核仍成立） | `get_components_static_params.details.data[]` 键为中文（`封装/尺寸/成本/所属厂商/模型id/重量`），`batch_query_component` 键为英文（`description/manufacturer/model/specs/type`）。根因在 EDI 返回体，仓库侧可做映射层 |
| P2-3 | 列器件工具信封不一致 | `list_schematic_components` 是 gRPC 任务信封（`details.component_count`）无分页；`list_simulation_components` 扁平结构（`total/offset/limit/component_type_counts`）带分页；`tr_query_schematic_components` 又是 TR 信封 → 基础层保留统一返回 + 分页，TR 版 docstring 点明「非 TR 流程用 `list_simulation_components`」 |
| P2-4 | 协议方法暴露为「伪工具」（备忘，非 bug） | `get_prompt`/`list_prompts`/`list_resources`/`read_resource` 是 MCP 协议方法非工具，服务端实测可用（`prompts/list` 9、`resources/list` 8）；Hermes 会话快照把它们暴露成 4 个「伪工具」——纯客户端展示问题，服务端修不了 |
| P2-5 | CSV 导出漏 `Out` + docstring 指向不存在的常量（仍复现） | ① `export_schematic_components_to_csv`（工程 23）输出仍含 `Out,Out1,,,,` 一行，而 `TermG`/`SParameter`/`Sweep`/`Var`/`HarmonicBalance`/`XDB`/`P_nToneG` 均已排除 → 按「端口不导出」口径 `Out` 也应入排除表（排除表在 EDI 侧）；② `design_export.py` docstring 提到 `EXCLUDED_TYPES` 但代码无此标识符——措辞需订正 |
| T5 | **两套返回信封** | EDA gRPC 家族业务数据在 `details`（带 code/hint）；本地/TR 家族直接放顶层且 `details=null`（实测：`get_project_summary`、`analyze_variables`、`get_signal_chain`、`list_simulation_components`、`list_eda_tasks`、`list_result_curves`、`get_simulation_component_schema`、`tr_*` 全家族）。收敛方案见 §八 |
| T3-尾 | 两处待拍板 | ① `create_project` 的 `path` 父目录无本地校验；② `switch_workspace` 现要求目录必须已存在（若原语义允许「指向未来目录、下次启动创建」则属收紧）。倾向见 §八 |
| P2-6 | 「`allow_extrapolation` 归属」措辞 + **能力缺口**（**真调后订正原判**） | `servers/eda/soft_ip.py:111`（docstring）与 `docs/TOOLS_API.md:877` 写「提供时**下游请求体**额外带上 `bandwidth` 与 `allow_extrapolation=false`」；**真调实测证明这不只是措辞问题**：① 不带 `bandwidth` → EDI `FAILED`「**缺少相对带宽（bandwidth,单位:%）**」⇒ 某些软 IP 该参数是**必需**而工具标为可选；② `bandwidth` 有 **EDI 侧按 `freq` 算出的有效范围**（本例 `freq=25` → **[1.2267, 6.27922]%**），超出即 `FAILED` 且提示「**如确需进行外推，请显式设置 `allow_extrapolation=True`**」；③ 但工具 payload **没有暴露 `allow_extrapolation`**（proto 文档：提供 `bandwidth` 时下行请求体固定带 `false`）⇒ **freq 落在有效带宽范围外时，MCP 侧无法请求外推 = 能力缺口**。**修法**：docstring 写明「部分软 IP `bandwidth` 必填」「超出有效范围会 FAILED」，并**评估是否把 `allow_extrapolation` 暴露为工具参数**（需 EDI 侧确认可传）；**代码本身无 bug** |
| P2-7 | **`allow_extrapolation` 未暴露（能力缺口）** | 下游在需要外推时要求显式 `allow_extrapolation=True`（见 `P2-6` 的真调证据②），而 MCP 工具无此参数、EDI gRPC 层在带 `bandwidth` 时固定写 `false` ⇒ **`freq` 超出软 IP 有效带宽范围时，MCP 侧无法完成下载**（只能换 freq）。建议：① 先确认 EDI 侧 gRPC 是否接受该字段；② 若接受则加为工具参数（默认不传 = 保持现状）；③ 若接受，`proto/grpc接口调用.md:611` 的「固定值」描述也要同步 |
| P2-8 | **CST 导出类工具不给链接**（口径不一致） | `cst_export_snp`（`cst/result_export.py:211`）只回 `{"success": true, "snp_path": "…", "message": …}` —— **无 `artifacts`、无 `download_url`**（`cst_export_farfield` 同族）；而另外 4 个产物类工具已同时给 token 链接 ⇒ 远程取 CST 产物要**两步**（先拿路径、再 `fetch_artifact`）。建议补 `artifacts` + `download_url`（每处 ~3 行）|
| P2-9 | **文档漂移 4 处**（本次产物 token 链接改造落地后产生） | ① `docs/HANDOVER.md:228`「报告自动返回 HTTP 预览链接（**10 分钟**有效）」→ 现为 **60 分钟**（`build_file_link` ttl=3600）；② `docs/HANDOVER.md:303`「open_document 生成 10 分钟 HTTP token，**仅本机 127.0.0.1 可访问**」→ 链接已按请求 `Host` 推导，**远程也可访问**；③ `servers/multimodal_vision/document.py:4` docstring「供 report 等模块注册文档 Token 并返回预览链接」→ 报告已改用 `build_file_link(disposition=…)`；④ **`direction` 已从 `get_signal_chain` 删除**（09-21，live `/tools/list` 已无该参数），但 `docs/TOOLS_API.md:606/616` 仍列着它、`docs/TOOL_INDEX.md`（生成物）需重生成 |
| P2-10 | **缺参/错参数名 → 回 SDK 裸 pydantic 文本**（影响**全部 91 个工具**，契约不一致） | 必填参数缺失或名字写错时，返回的是 `isError=true` + `1 validation error for <tool>Arguments … Field required [type=missing] … errors.pydantic.dev/…`，**不是**统一信封（无 `success`/`error_code`/`hint`）。**实测 5 例（2026-09-21）**：`get_simulation_component_schema`（缺 `component_type`）、`get_hfss_analysis_status`（缺 `task_id`）、`tr_read_guide`（缺 `guide_name`）、`search_public_models`（把参数名写成 `sub_type_id`，实际要 `sub_type`）、`get_signal_chain`（09-21 删除 `direction` 后，仍传该参数的老调用方**直接命中此路径**）。**根因**：FastMCP 在**调用函数之前**做参数校验，本仓函数体里的 `require_*` 守卫根本没机会执行。**可行的修法**：对高频工具把必填参数改为「可选 + 函数内 `require_nonempty` 校验」，即可回到统一信封（每工具 1–2 行）；否则 agent 只能去解析 pydantic 原文 |

---

## 八、待你拍板（我给倾向 + 理由）

1. **T3-尾 ① `create_project` 的 `path` 要不要本地校验？** → 倾向**要但只做最小校验**（非空时校验父目录存在/可写，不存在 `INVALID_PATH`，不新建目录）；或退一步——不校验但把语义写进 docstring。理由：`create_workspace` 已因「垃圾值真写入」挨过一次（T3）；传错路径时你想要「报错」还是「默默在 `C:/` 建工程」？
2. **T3-尾 ② `switch_workspace` 要求目录已存在？** → 倾向**保留收紧**。理由：EDI 侧实测「指向不存在目录」时根本没落文件，本地拒绝只是更早暴露问题；唯一风险是 EDI 将来支持「指向未来目录」会挡路，那就 docstring 写明「目录必须已存在」。
3. **T5 两套信封 → 分两步**：① 先统一「失败」信封（所有家族失败一律 `error_response(...)`，ANSYS 已迁完）→ agent 判失败只看 `success`；② 「成功」信封暂不合并——EDA gRPC 家族 `details` 带 `status/task_success/outcome_known/hint`，本地/TR 家族没有任务语义，强行统一只会造出「永远为 null 的字段」，改为 docstring 首行写清「本地计算，不产生 task 信封」。
4. **S1 契约口径 → 用别名过渡，别一步改签名**：先 `originalid_list` 加别名 `original_ids`（两者都收）→ docstring 只写新名 → 下轮删旧名。直接改签名会打断现有会话/脚本。 **同类新例（09-21 已发生）**：`get_signal_chain` 的 `direction` 参数被**直接删除**（live `/tools/list` 已无此参数）—— 现有客户端/脚本若仍传 `direction`，会拿到 SDK 裸 pydantic 校验错（见 `P2-10`）。**我的倾向**：删得对（它本来就是「撒谎接口」，`backward` 未实现却回显）；只要你确认现场没人传该参数，**保持现状即可**，只需同步 `docs/TOOLS_API.md:606/616` 与重生成 `docs/TOOL_INDEX.md`（=`P2-9` ④）；若不确定，就按 S1 思路过渡一版（保留参数 + 忽略 + docstring 标废弃），下轮再删。
5. **R11 定性 → 倾向降为「观察项」**：`_COM_PROGIDS` 两项都会 `try`，失败抛的是文档化的 `RuntimeError`（不是崩溃）；「多 ProgID 兜底是否真走不通」只能在 AEDT 环境实证，本仓库侧判不了。若你手边正好有 AEDT 未运行/已运行两种状态，测一次 `query_desktop_state()` 就能定案。
6. **扫描范围口径 → 已按实测改为「98 个 .py / 全仓 180 文件 / 18,890 行」**（原「133 文件」口径不明）；若你记得 133 的口径（例如含 docs 的 `.md`），告诉我我改回更准确的表述。

---

## 九、明确不动项 + 端到端 smoke 方案

### 9.1 明确不动（已逐条定性）

| 不做 | 理由 |
|---|---|
| 为「数量好看」合并工具（90→88 够，别追 50） | §6.1 三条判据；判据 3（不产生互斥可选参数）本身会挡住大多数 |
| 删「词典/公共 API/防御冗余」（`ERROR_CODES`、`TaskRunner.count/prune`、`eda/__init__` re-export、`load_dotenv` 重复、`check_pyd_imports.py` 手写 PE 解析） | 查表完整性 / 对外契约 / 安全冗余 / 刻意零依赖 |
| C1 现在合并两套任务队列 | 先给 `TaskRunner` 加进度回调、动仿真主链路，风险 > 收益 |
| C2 删 17 个 TR wrapper | docstring 就是客户端 tool description |
| B7 合并 `resources_prompts` 7 文件 | 口味不是问题，按语义拆可读 |
| 动真实信任边界（`report/generator` 校验链、`chat/routes` 上传限制、`_prepare_parameters` 校验） | 这些「冗余」是安全边界，不是过度工程 |
| 只改测试口径数字、不做 `skipif` 分类 | 只把 413 写成「含跳过」没用，要真加分类才能分辨真跑（现已有 `real_project` fixture + `HAS_PROJECT` 分类） |
| 删 `execution_mode` 参数（原 R27） | docstring 已注明「已废弃（仿真仅支持异步），保留参数兼容旧调用」——**文档化的兼容参数**，不是死代码 |

### 9.2 端到端 smoke 具体方案（未落地，方案已跑通）

> ⚠️ **当前盲区**：全仓**没有任何测试真起 MCP app、真调一次工具**（`TestClient` 只出现在 `test_process_guard.py` 的**合成** Starlette app 上，路由是桩 `ok`）。建议补 1 条：进程内构造真 app → 数一次 `tools/list` → 调 2 个只读工具（`list_ideal_components` / `get_service_status`），标 `@pytest.mark.e2e`、默认 skip（`RUN_E2E=1` 才跑）—— 既不拖慢 CI，又能在交付前手动跑一次。**理由**：分类解决的是「真跑/跳过可分辨」，但**「服务真起来后能不能调通」目前仍无自动化覆盖**。

```python
# tests/test_e2e_smoke.py
from __future__ import annotations

import asyncio
import os
import re
import socket
import subprocess
import sys
import time
from pathlib import Path

import pytest

REPO = Path(__file__).resolve().parents[1]
TOOL_INDEX = REPO / "docs" / "TOOL_INDEX.md"


# ── Tier 1：进程内（默认跑，~0.2s，不需要 EDI 在线）────────────────────
def test_tool_registry_matches_tool_index():
    """工具清单与 docs/TOOL_INDEX.md 一致 —— 清单自动生成，是单一事实源。"""
    import servers.registry_server  # noqa: F401  触发注册
    from servers import mcp

    md = TOOL_INDEX.read_text(encoding="utf-8")
    declared = int(re.search(r"共 (\d+) 个工具", md).group(1))
    listed = len(re.findall(r"^- `", md, re.M))
    tools = asyncio.run(mcp.list_tools())          # 走公开 API，含 inputSchema 生成

    assert len(tools) == declared == listed, f"注册 {len(tools)} / 声明 {declared} / 条目 {listed}"
    assert all(t.description for t in tools), [t.name for t in tools if not t.description]
    assert all(t.inputSchema.get("type") == "object" for t in tools)


def test_local_readonly_tool_callable_in_process():
    """真调一个纯本地只读工具（不需要 EDI），验证信封可解析。"""
    import servers.registry_server  # noqa: F401
    from servers import mcp

    r = mcp._tool_manager._tools["get_service_status"].fn()
    assert isinstance(r, dict)
    # ⚠️ 本工具返回「裸状态 dict」，**没有 success/error_code**（T5 第三套信封），
    #    别照抄 assert r["success"] —— 那是 EDA gRPC 家族的形状。
    assert {"grpc_target", "channel_state"} <= set(r)


# ── Tier 2：真起服务进程 + HTTP/MCP 握手（RUN_E2E=1）────────────────
def _free_port() -> int:
    with socket.socket() as s:
        s.bind(("127.0.0.1", 0))
        return s.getsockname()[1]


@pytest.mark.skipif(os.environ.get("RUN_E2E") != "1", reason="需 RUN_E2E=1（会真起服务进程）")
def test_http_end_to_end_smoke():
    import httpx
    from mcp import ClientSession
    from mcp.client.streamable_http import streamable_http_client   # 1.28 起的新名

    port = _free_port()
    env = {**os.environ}
    env.pop("PYTHONPATH", None)
    env["MCP_ALLOWED_PROCESSES"] = ""     # ★关键：不清空则被 process_guard 拦成 403

    proc = subprocess.Popen(
        [sys.executable, "start_servers.py", "--port", str(port)],
        cwd=str(REPO), env=env,
        stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
    )
    try:
        deadline, ready = time.time() + 60, None
        while time.time() < deadline:                     # 等 /ready（启动中返回 503）
            try:
                resp = httpx.get(f"http://127.0.0.1:{port}/ready", timeout=2)
                if resp.status_code == 200:
                    ready = resp.json()
                    break
            except Exception:
                pass
            time.sleep(0.5)
        assert ready is not None, "/ready 未在 60s 内就绪"
        assert ready.get("tool_count", 0) > 0 and ready.get("tools_hash")

        async def run() -> tuple[int, bool]:
            async with streamable_http_client(f"http://127.0.0.1:{port}/mcp") as (rd, wr, _):
                async with ClientSession(rd, wr) as session:
                    await session.initialize()
                    tools = await session.list_tools()
                    res = await session.call_tool("get_service_status", {})
                    return len(tools.tools), bool(res.isError)

        n_tools, is_error = asyncio.run(run())
        assert n_tools == ready["tool_count"]     # 传输层与 /ready 自述一致
        assert is_error is False
    finally:
        proc.terminate()                          # ★必须收尾，否则残留进程占 EDA 执行槽
        try:
            proc.wait(timeout=15)
        except subprocess.TimeoutExpired:
            proc.kill()
```

**三个坑（都踩过了）**：

| 坑 | 现象 | 处置 |
|---|---|---|
| 进程白名单 | 客户端（python.exe）访问 `/mcp` 被 `ProcessWhitelistMiddleware` 拦成 **403** | 子进程环境清空 `MCP_ALLOWED_PROCESSES`（实测该 env 名可覆盖配置）；这也是「curl 直连被挡死」的同一个机制 |
| 客户端 API 改名 | mcp **1.28** 起 `streamablehttp_client` 触发 `DeprecationWarning: Use streamable_http_client instead` | 用 `streamable_http_client`（实测无警告、结果一致） |
| 信封形状差异 | `get_service_status` 返回**裸 dict**，无 `success` 键 | 断言按键集合；需标准信封的调用另选（那类要 EDI 在线，放 Tier 2） |

**为什么值得做**：现有 430 条全是**进程内单元/集成**测试，**没有任何一条真起过 app、真走过 HTTP 传输层**——而「工具注册能不能装配成 app」「就绪门与白名单会不会把客户端挡在外面」恰恰是交付现场最容易翻车的地方（Tier 2 只花 3.5s）。


---

## 十、接口收敛路线（工具合并，降认知成本）

> 结论均基于**真实签名**（`inspect.signature`，2026-09-16）。

**合并判据（三条同时满足才合并）**：① 后端同一个（同一 gRPC action / HTTP 端点）；② 参数集高度对称（重叠 ≥ 80%）；③ 合并后不产生互斥可选参数。

| 候选 | 结论 | 依据 |
|---|---|---|
| `use_schematic_from_library_create_project` + `..._import` | ✅ **合并** → `use_schematic_from_library(file_uuid, mode, project_path="")` | 只差一个参数，选错有后果（新建 vs 导入） |
| `simulate_netlist` + `simulate_netlist_with_ads` | ✅ **合并** → `simulate_netlist(netlist_path, ads_path="", ...)` | 只差 `ads_path`；timeout 默认值不一致（600 vs 120）正好统一 |
| `search_public_models` + `search_personal_models` | 🔸 不合并，只 docstring 互指 | 签名完全一致但判据 3 不满足：合成 `library=` 会静默取错源 |
| `search_schematic_*_public` + `..._personal` | 🔸 不合并 | 同上 |
| `add_ideal/perf/create_simulation_component` | ❌ 不合并，改命名族化 | 数据源与必填项各异，合并成互斥可选参数巨型工具 |
| `list_schematic_components` / `list_simulation_components` | ❌ 不合并，但参数对齐 | 前者无 offset，后者有；以后者为模板统一 |
| `export_project_netlist` / `tr_get_project_netlist` 等 | ❌ 不合并 | TR 版是版本化可追溯工作流，重叠是故意的，docstring 互指 |
| `get_simulation_async_status` / `..._result` | ❌ 不合并 | 一个是轮询、一个取大 payload |
| `open/close_edi_project` vs `open/close_hfss_project`；`cst_*_query` vs `get_hfss_*` | ❌ 绝不合并 | 不同工具链后端；真问题是命名漂移 |

**净结果 90 → 88（只减 2）**。若执行合并：属 breaking change，保留旧名 alias 过渡；同步点 `scripts/gen_tool_index.py`（重生成 `TOOL_INDEX.md`）、`tests/test_tool_index.py`、`docs/TOOLS_API.md`、`chat/index.html` 工具映射。

**比合并更划算的三件事**：① 命名族化（`add_*`/`list_*`/`tr_*` 统一前后缀，docstring 首行写清来源/后端）；② 参数语义对齐（列/搜类统一 `limit`/`offset`/`summary_only`）；③ `TOOL_INDEX.md` 分组渲染（已自动生成）。

**待优化（保留但改进）**：异步查询命名三套不一致（CST `*_query` / HFSS `get_*_status` / EDA `get_*_async_status`；`cst_export_snp` 无 query、`cst_export_farfield` 有 query）；模型搜索仍无 `offset`（软 IP `search_*_soft_ip_models` 已由服务端自动合并分页，可作参照）。

---

## 十一、依赖侧 / 环境（本仓库改不了，仅跟踪）

| # | 问题 | 说明 |
|---|---|---|
| P0-1 | SimulationAgent 密钥明文 argv | EDI 侧修复（§三） |
| T9 | `analyze_image` 当前不可用：视觉模型请求 `SSL: UNEXPECTED_EOF_WHILE_READING` | 疑端点/证书/网络。先分 `VISION_BASE_URL` 可达性 + 证书链各测一次；注意 `analyze_image`（`VISION_*`）与 `show_image`（`IMAGE_ALLOWED_DIRS`）是两套独立配置 |
| N4 | EDI GUI 的 ADS 路径：`config.ini` `[ads] hpeesof_dir` 现值正确，但仍需 GUI 设置同指，否则 EDI 启动重写后复现 | 备份 `config.ini.bak-20260910` |
| N6 | Hermes 客户端旧服务器名 `edi-mcp-sse-50026` 残留 `state.db`（114 次）/`profiles/0/state.db`（46 次）/`cache/mcp_schema_cache.json`，每轮重启多一次无谓连接 | `hermes mcp list` 只有 `eda` 一个，配置本身干净 |
| P2-2/3/5 根因 | EDI 返回体键语言、`Out` 排除表 | 仓库侧做映射层/措辞订正（§七），根因在 EDI 侧 |

---

## 十二、审计方法论（供下轮复用）

1. **别用 Hermes 的 shell 环境判定「编码/环境类修复」**：Hermes 工具 shell 自带 `PYTHONUTF8=1`+`PYTHONIOENCODING=utf-8`+`PYTHONPATH=<hermes>`，会把依赖 locale 的修复判成失败。判定法：`env -u PYTHONUTF8 -u PYTHONIOENCODING` 再跑对照，两种环境结论不同 = 修复绑在环境上，本身就是 bug 信号。
2. **判定「中文是否可读」用布尔 + 落文件，别信屏幕文字**：清掉 `PYTHONIOENCODING` 后 stdout 变 cp936，重定向回 UTF-8 管道会把正确中文显示成乱码。
3. **每类探针用互不干扰的独立路径**：曾被 `create_workspace` 先建出的目录掩盖过 `list_epp_projects` 抛异常（「文件夹不存在」探针假通过）。
4. **探针 helper 形参别叫 `name`/`path`**：与工具参数撞名会在调用处 `TypeError`，用 `tool` 等中性名。
5. **编码问题别靠「先试 UTF-8」的直觉——两种编码可能双向歧义**（2026-09-17 `R4-残` 实测）：GBK 汉字有 **9.2%（1,920/20,902）** 的字节序列**同时是合法 UTF-8**，此时 `try utf-8` 不抛错、**静默解错**（`'专业'`→`'רҵ'`）；反过来 UTF-8 中文字节在 cp936 下也照样能解（`'涓撲笟'`）。**判据**：先认 **BOM**（唯一无歧义信号），无 BOM 时**按来源定顺序**（本机 Windows 生成 → cp936 优先；上游 UTF-8 → 不该进这个 helper）。**「先 utf-8 更保险」是错的直觉**——早期给的建议就栽在这上面；现实现见 `servers/utils.py::decode_local_text`（BOM → cp936 → utf-8）。

6. **「看起来像」的结论一律真调复核**：多轮改判（`search_*` 不合并、`limit` 静默失效、`compare_simulation_results` 从未可用）全来自实测而非阅读。本报告的多处改判（`search_*` 不合并、`limit` 静默失效、`compare_simulation_results` 从未可用、`R19`「永远排队」实为「最多阻塞 TTL」……）**全部来自实测、而非阅读**——**子代理读码的命中率约 90%，但那 10% 会误导修复方向**。能真调的一律真调（broken symlink、GBK 文件、`timeout=1`、坏目录都是真造出来的），比读码强得多。

7. **功能性问题必须真调，但先钉死运行态——否则会把竞态写成缺陷**（2026-09-21 本轮）：逐条 `curl` 打 `/mcp` 真调 **28 个工具 + 4 个协议方法**，结论是**无功能性失效**（要么返回真数据，要么回干净统一信封）。踩到的坑：EDI 恰在扫描中途启动（`50055` pid 使 `/ready` 由 `offline` 变 `online`），`get_service_status` 报 `channel_state: unknown` 而资源报 `ready`，一度被写成「三处口径矛盾」→ 复核为**竞态**（状态字段是惰性缓存，见 §13.6）。**下轮复用**：
   - **先钉运行态再开测**：`netstat -ano | grep -E ':50055|:17866|:17867'` + `GET /ready`，确认 EDI（`50055`）/SimulationAgent（`17866`）/渲染服务（`17867`）在不在线，再决定哪些工具「失败是预期」。
   - **本轮已真调通过**：本地/静态 —— `get_service_status`、`list_eda_tasks`、`list_ideal_components`、`get_model_category_params`、`get_current_workspace`、`list_epp_projects`；工程读取 —— `get_project_summary`、`analyze_variables`、`list_schematic_components`、`list_simulation_components`、`get_schematic_component_info`、`get_signal_chain`（空工程回干净 `NETLIST_EMPTY`）；EDA gRPC —— `batch_query_component`、`get_components_static_params`、`search_public_models`、`search_soft_ip_categories`、`tr_query_components`；任务/查询 —— `cst_solve_query`、`cst_export_farfield_query`（假 task 回干净 `TASK_NOT_FOUND`+`do_not_retry`）、`get_hfss_project_info`、`tr_get_workflow_state`、`tr_get_simulation_capabilities`；产物/文档 —— `open_document`（`link`）、`fetch_artifact`、`generate_simulation_report`（真生成 113 KB PDF）；协议 —— `prompts/list`（9）、`prompts/get`（正确参数）、`resources/list`（8）、`resources/read`。
   - **本轮未真调（需写类/长跑/环境，别当成「已通过」）**：`simulate_*` / `*_async` 全家族、`simulate_anti_burnout`、`tr_run_simulation` / `tr_execute_simulation_plan` / `tr_modify_netlist` / `tr_parse_raw` / `tr_prepare_report` / `tr_generate_document` / `tr_sync_project_components` / `tr_restore_schematic`、CST 写类（`cst_solve_async` / `cst_export_snp` / `cst_export_farfield`）、`download_soft_ip_model`、`load_performance_component_from_mms`、库导入/改工程类（`use_schematic_from_library_*`、`replace_*`、`generate_schematic_from_netlist`、`add_*` / `update_*` / `delete_*` / `set_component_active_state`）、`capture_schematic`、`export_project_netlist`、`open_edi_project` / `close_edi_project` / `open_hfss_project` / `close_hfss_project`、`launch_edi` / `launch_aedt`、`show_image` / `analyze_image`（后者在上一轮 offload 改造的真调中验过一次，本轮未复测）、`search_schematic_from_*`、`search_personal_models`、`search_public_soft_ip_models` / `search_personal_soft_ip_models`、`open_document`（`local` 模式）。

---

## 十三、处置建议（未修项的可执行修法）

> 只覆盖**未修**项：怎么修、改哪个文件、大概几行。原则沿用前几轮 —— **在入口/helper 处一次收敛**（`require_*` 家族 + 统一 `error_response`），而不是 N 处 try/except。

### 13.1 启动自检（`R46-残`）/ 任务日志（`R54`）/ 日志（`R47` / `R49`）

**问题（已收窄）**：09-21 那批已补上**任务日志**（`task=… status=QUEUED/RUNNING/SUCCEEDED/FAILED`）、**守卫/探针落日志**（`GUARD mode=…`、`process-probe … exe=… cmd=…`）、**启动自检**（孤儿进程 WARN + 「重启会丢内存态任务」摘要）—— 10:11:08 重启后**日志实测全部在位**。剩下的只是三处**漏检/顺序**（下表）。对照：EDA（ADS 仿真）侧的 `simulation.py` 日志格式仍是最完整的范本。

**修法（剩三件，~8–12 行）**：

| # | 改动 | 位置 | 验收 |
|---|---|---|---|
| 1 | **自检过滤器改子串/前缀匹配**：`ansysedt.exe` 保留；CST 改为覆盖 `cstd.exe` / `CSTDCMainController_AMD64.exe` / `CSTDCSolverServer_AMD64.exe`（如 `name.lower().startswith("cst")`）；顺手扫 `*.aedt.lock`（死 PID 才动，活跃 PID 跳过）。**实测依据**：09-21 10:11 自检对 3 个常驻 CST 进程（09-04 起）零 WARN | `start_servers.py::_startup_self_check` | 开着 CST 重启一次 → 日志出现 WARN |
| 2 | （可选，谨慎）把拉起的软件进程登记进句柄表，**仅正常退出**时清理（`ansys/project_manage.py:154/349` 的 `Popen` 目前**未绑**任何句柄）| `ansys/project_manage.py` | 只在正常退出路径清理；**不要**在异常退出时强杀（可能中断正在进行的求解）|
| 3 | **`R54` 日志修正**：`_log.info("…QUEUED…")` 上移到 `self._executor.submit(...)` **之前**（现在是之后 → 快任务顺序颠倒）；`submit` 返回 `None` 的两条路径各加一行 `WARNING`（`reason=busy` / `reason=max_tasks`）；`FAILED` 改 `WARNING` +`exc_info=True`；`except Exception: pass` 改 `_log.debug("自检跳过: %s")` | `servers/task_runner.py` / `start_servers.py` | 连提两个任务 → 日志按 QUEUED→RUNNING→终态顺序出现 |



**`R47` / `R49` / 文档指针（按性价比排序）**：
1. **`R47` 只做日志卫生（可选，0 代码风险）**：日志按启动周期切分（文件名带 `pid`/启动时间），或启动时打一行「本次启动，历史记录请按日期甄别」。**不建议**为此改 SDK 的 prompt 渲染（属上游行为，本仓加校验会与 SDK 重复）。
2. **`R49` 观察，先不动**：`loop.set_exception_handler` 吞 `ConnectionResetError` 是**全局**副作用（可能掩盖真实断连原因），而远程/VPN 场景断连本就频繁 —— 除非日志噪音实际影响排查。
3. **文档指针（0 代码）**：`tests/test_remote_access.py` 的 docstring 仍写「对应『远程操作方案』P0 第 2/3 项」，而该文档已在工作树中删除（`docs/远程操作方案.md`，**暂存态删除**）→ 改指向 `docs/审查报告.md` §13.3 或 `docs/HTTP_API.md`「访问控制与远程部署」节。

### 13.3 远程：鉴权暂缓决定（`R42`）与加固待办

**`R42`（🔸 本期暂缓 —— 用户 2026-09-18 决定「直接访问、内网可信」；原型已备好，后续一步可加）** —— 三选一：
1. 若需身份鉴权：新增 ~25 行 Bearer 中间件，**必须覆盖 `/mcp` + `/upload` + `/chat` + `/tools/list`**（后三者当前连白名单都不管）；
2. 若不需要 `/chat`/`/upload` 远程可用：**关掉或纳入保护**（暴露面立刻收窄一半）；
3. 白名单留空时**连探针一起关**（现在每请求扫一遍 `psutil.net_connections` 并 print，远程下纯噪音 + 开销）。

**`fetch_artifact` / token 链接的边界（加固待办，未做）**：

- ⚠️ **无「允许根目录」限制**：`_register_any_file_url` 不校验路径 ⇒ 它是**任意可读文件的下载通道**（实测 `C:/Windows/win.ini` 可取）。无鉴权下风险放大，建议加配置（默认工作区，留空=任意）。
- ⚠️ **`/documents/{token}`、`/images/{token}` 不要纳入 Bearer 保护**：主机浏览器/`curl`/下载器直接拉 URL，**带不了 `Authorization` 头**；这两个通道靠**不可猜 token**（`token_urlsafe(24)`）+ 短 TTL 自鉴权，是刻意设计。
- ⚠️ **`TaskRunner` 不复制 context**：`concurrent.futures.ThreadPoolExecutor`（CST/HFSS 异步任务）**不像 `anyio.to_thread`** —— 实测 worker 里读不到请求 Host ⇒ 「异步任务完成后自动注册产物链接」会给出**打不开的链接**，需要 `MCP_PUBLIC_BASE_URL` 兜底（或把提交时的 Host 存进 task metadata）。

**✅ 已实测（2026-09-20 17:4x，对 `tool_count=91`/`tools_hash=fff550e2` 的真实服务逐条真调；末两行为 09-21 10:16 重启后复测）**：

| 检查 | 结果 |
|---|---|
| `fetch_artifact` 取 `README.md` | `success=true`、`size_bytes=24745`、`sha256=c6e6e490…cf06`、`expires_in=600` |
| `GET /documents/{token}` 回读 | **200 且 sha256 与原文件完全一致**，`content-disposition: attachment` |
| **经 `http://192.168.0.58:50026/mcp` 调用** | 返回链接的 host **就是 `192.168.0.58`** ⇒ **offload 到工作线程后 Host 推导仍正确**（此前标「未能验证」的项闭环）；该 LAN 链接 `GET` 200 |
| 坏 token | `404` |
| `ttl_seconds=1` | 钳到 **30** |
| 文件不存在 | `FILE_NOT_FOUND` + `do_not_retry`（干净信封） |
| 中文文件名产物（`RAW 转图像工具使用说明.txt`） | `file_name` 中文正确、`size_bytes=4430`、`sha256=5dea213f…` |
| `open_document` 非白名单 `.md` | 仍 `UNSUPPORTED_FORMAT` ⇒ **白名单未被放宽** ✓ |
| `open_document(link)` 白名单内 `.txt` | 返回 token URL（非 `file://`）+ `display_mode=inline` |
| 回归套件 | **430 passed / 2 skipped / 0 failed** |
| **（09-21 10:16 重启后复测）报告 PDF 链接** | `preview_url`/`markdown_link`/`message` **三处同一个 token URL**；`GET /documents/{token}` → **200**、`content-disposition: inline; filename="probe_report.pdf"`、sha256 与本地文件**逐字节一致** |
| **（09-21 10:16）报告 DOCX 链接** | `content-disposition: attachment; filename="probe_report.docx"`、`content-type: application/vnd.openxmlformats-…wordprocessingml.document` ⇒ `build_file_link` 的 `disposition` 透传**生效**（否则 DOCX 会被悄悄改成 `inline`）|

**剩余成本**：`R42` 本期 0 行（暂缓，后续 ~25 行）；上述加固（允许根目录）≈10 行。

### 13.4 软 IP 下载的能力缺口（`P2-6` / `P2-7`）

**真实下载验证（2026-09-20，EDI 已启动；软 IP `e7266a6e-3ffd-4df2-ba00-7ce4e53d1e08`，`freq=25`）**：

| 调用 | 结果 | 落盘 |
|---|---|---|
| 不带 `bandwidth` | `success=false` / `status=FAILED` / `hint=task_failed` /「**缺少相对带宽（bandwidth,单位:%）**」 | ⛔ 无文件 |
| `bandwidth=10` | `FAILED` /「相对带宽 10% **超出有效范围 [1.2267, 6.27922]%**；请通过请求体字段 bandwidth 指定该参数。如确需进行外推，请显式设置 `allow_extrapolation=True`」 | ⛔ 无文件 |
| **`bandwidth=5`（范围内）** | ✅ `success=true` / `status=SUCCEEDED` / `hint=ok` /「soft IP model downloaded」 | ✅ **291,906 字节**，`sha256=b27feccf…`，头 `$begin 'AnsoftProject'` |
| **`bandwidth=1.5`（范围内）** | ✅ 同上 | ✅ **291,908 字节**，`sha256=ef269930…` |

**顺带证实 `bandwidth` 真的改变模型内容**（不是只改元数据）：两文件在字节 **9411** 处首个差异 —— `VariableProp('p', 'UD', '', '2.582317566777495mm')`（bw=5） vs `'2.266521204029607mm'`（bw=1.5），即 **几何变量 `p`（周期）随相对带宽变化** ⇒ 下载的确是按「频率 + 相对带宽」重新生成的模型。

**另一处口径提示**：成功响应的 `result_path` **恒为空串**，真实产物路径在 **`details.save_path`**（与 docstring 一致）。agent 若按其他工具的习惯统一读 `result_path` 会拿不到路径 → 建议在 docstring/`TOOLS_API.md` 里显式提示「**产物路径读 `details.save_path`，不要读 `result_path`**」。

**处置建议**：`P2-6`（`soft_ip.py:111` docstring + `TOOLS_API.md:877` 两处措辞，并写明「部分软 IP 的 `bandwidth` 是**必需**」「超出有效范围会 `FAILED`」）**0 代码风险，可立即做**；`P2-7` 需先向 EDI 侧确认 gRPC 是否接受 `allow_extrapolation`，接受再加为工具参数（默认不传 = 保持现状），并同步 `proto/grpc接口调用.md:611` 的「固定值」描述。

### 13.5 一行级小改清单

| 项 | 修法 | 量 |
|---|---|---|
| `R23` | `action_accepted` 在可达路径恒真（现为 `= True` + 注释说明）→ 删变量，或按现状保留 | 1 行 |
| `N1` | `process_guard.py:188-189` 两行注释掉的调试日志（`# _log.info(...)` / `# print(...)`），删 | 2 行 |
| `register_document_url` | 生产零调用（保留为公共 API）→ 订正 `document.py:4` docstring 与模块 docstring 的「供 report 等模块注册」措辞 | 1 行 |
| `P2-8` | `cst_export_snp` / `cst_export_farfield` 补 `artifacts` + `build_file_link(...)` 的 `download_url`（口径与另 4 个产物类工具对齐）| 每处 ~3 行 |
| `P2-10` | 高频工具的必填参数改「可选 + 函数内 `require_nonempty`」，使缺参也回统一信封（其余工具保持现状）| 每工具 1–2 行 |
| `P2-9` | 文档漂移 4 处（见 §七），0 代码风险；**注意 `TOOLS_API.md` 的 `open_document` 条目写的 10 分钟仍是对的**（它走 `register_document_url`，TTL 600） | 1 行 × 3 |
| `P2-5` | ① `Out` 仍被导出（排除表在 EDI 侧，证据见 §附）；② `design_export.py` docstring 提 `EXCLUDED_TYPES` 但代码无此标识符 → 订正措辞 | 注释级 |

### 13.6 观察项 / 只加注释

| 项 | 处置 |
|---|---|
| `R11` | 降为**观察项**：多 ProgID 兜底是否真走不通需 AEDT 环境实证（失败路径本身是文档化的 `RuntimeError`） |
| `R14` | 保持现状 + 注释说明「`pending_count()>0` 只能近似区分 `ANALYSIS_BUSY`/`TASK_LIMIT_REACHED`」（精确区分需改 `TaskRunner` 接口，收益低） |
| `R4-残` | **已接受的已知代价**：`decode_local_text` 的 docstring 已声明「不是通用解码器」；「UTF-8 无 BOM」会被 cp936 静默解错，**是否错字取决于具体字节**（GBK 汉字有 ~9.2% 的字节序列同时是合法 UTF-8） |
| `R20` | 实质问题（确认门空壳）交叉登记到 `TR集成问题清单.md`，本报告只留位置 |
| `get_service_status.channel_state` | **已核实非缺陷**（2026-09-21）：该字段是**惰性探测的缓存值**，EDI 恰在探测间隙启动时会滞后报 `unknown`/`channel_cached=false`，而资源 `edi://service/status` 同刻已报 `ready` ⇒ 曾被误判为「三处口径矛盾」，复核为**竞态**。建议只在 docstring 加一句「此字段为缓存值，非实时探针」 |
| `compare_results` 的 `img_ok/csv_ok = True` | 09-21 改动：去掉 `Path(...).exists()` 落盘求证 —— 与旧行为**等价**（`savefig`/`open` 失败即抛），只是少了防御性校验。**建议保持现状**；若要防御可改 `Path(output_path).stat().st_size > 0`（1 行） |
| 启动横幅的 `Guard:`/`Probe:`/`Note:` 三行 | 09-21 起**只写日志**（原来是 `print`）—— 文档未引用这三行，**无漂移**；日志里反而是**新增**（`GUARD mode=whitelist\|probe\|whitelist_ignored_remote`）。**建议保持现状**；若现场习惯看控制台确认白名单状态，可再 `print` 一份 |
| `R21-残` | 名单已与实际参数名对齐（`component_id`→`component_uuid` ✓、`result_paths` 死键已删 ✓）；仅剩 `isinstance(val, str)` 使 **list 类参数不做空检查** —— **建议保持现状**（工具自身会校验），若要做则改「参数名 → 是否要求非空」声明表 |

### 13.7 结构性 / 契约

- `C1`（两套并行任务队列）/ `C2`（TR wrapper 漂移）→ §六；两处拍板项（`T3-尾`）与倾向 → §八。
- `T5` 两套信封（先统一失败信封）、`P2-5`、`P1-*` 参数命名、`S1` 别名过渡 → §七 问题 + §八 倾向。

### 13.8 非代码动作项（不是代码问题，但别忘）

| # | 动作 | 触发条件 / 说明 |
|---|---|---|
| 1 | **重打包远程机那只 exe** | 旧 exe 没有第 91 个工具 `fetch_artifact`（`tools_hash` 仍为 `ae6cd062`/90）⇒ 远程取产物在旧包上不可用 |
| 2 | **打包后同步 `docs/WIN7部署与启动手册.md` 基线** | 由 `90`/`ae6cd062` 改为 `91`/`fff550e2`，并在 Win7 现场复测冒烟 |
| 3 | **刷新 Hermes 客户端的 MCP 连接** | 新增第 91 个工具后，本会话里 `tool_call` 仍认不到 `fetch_artifact`（清单未刷新）⇒ 客户端要重连才能直接调用（`hermes mcp test local-mcp`）|

---

## 附：证据留档（仅未修项）

`export_schematic_components_to_csv`（P2-5：`Out` 仍漏导，工程 23）：

```text
original_model_type,original_model_name,original_model_id,alternative_model_type,alternative_model_name,alternative_model_id
Out,Out1,,,,
NC10355C_2931,NC10355C_29311,36742914-1237-4530-baef-488e4765a920,,,
```

`get_components_static_params` vs `batch_query_component`（P2-2：键语言不一致）：

```text
get_components_static_params.details.data[0] 键: ['封装', '尺寸', '成本', '所属厂商', '模型id', '重量']
batch_query_component.details.data[0] 键: ['description', 'manufacturer', 'model', 'specs', 'type']
```
