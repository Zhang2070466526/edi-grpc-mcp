# EDI MCP 服务审查报告（未解决问题清单）

- **最后更新**：2026-09-17（第八轮：7 子代理静态扫全仓 / 第九轮：R 系列逐条复核，整合前七轮，已修项清出）
- **扫描范围口径（第九轮实测核对）**：`.py` **98 个**（`servers/`+`tests/`+`scripts/`+`start_servers.py`）、全仓文件 **180 个**（排除 `.venv`/`build`/`dist`/`__pycache__`）、**18,890 行**。原写「133 文件」口径不明（可能含 `.md`/`.proto`/`.ps1`/`.bat`），已按实测标注
- **对象**：EDI MCP 服务（`http://127.0.0.1:50026/mcp`）90 个业务工具 + 仓库源码
- **方法**：① 运行形态侦察；② 全量 `tools/list` 审查；③ 行为抽样探针（真调 + 白名单 + 进程内直调）；④ 读源码 `grep` 复核；⑤ 7 子代理并行静态扫全仓 + 父代理汇总
- **标签**：`[崩溃]` 穿透 MCP 边界的异常；`[死代码]`/`[精简]`/`[结构]` 过度工程；`[契约]` 接口/命名/文档不一致；`[环境]` 本机运行环境；`[依赖侧]` 本仓库改不了、仅跟踪
- **口径**：只留**未解决**项；已修项删除（历史见 git log 与删除前快照 `C:/Users/JGL/审查报告-删除前快照-*.md`）。判「死代码」须区分**死代码路径**（函数/字段/分支，删）与**词典/公共 API/防御冗余**（查表完整性/对外契约/安全冗余，留）
- **交叉参考**：TR 集成实质问题（会话归属串台、确认门空壳、17 工具认知负担）→ `servers/tr_simulation/TR集成问题清单.md`，本报告只收其文档漂移类条目
- **状态**：50026 未运行（人为停止）；源码侧健康（in-process 加载 90 工具 / 8 资源 / 9 Prompt，套件 413 passed / 1 skipped）。**前七轮的「agent 必然踩且无法自愈」行为 bug 已清零**（T1–T4 / T6 / T11 / T13–T16 逐条实测闭环）；**第八轮静态审查新发现 R1–R40，已逐条复核（台账见 §十三）：剔除 2 条误报（R1/R2）、订正 4 条描述（R13/R19/R21/R25）、2 条改判（R11→观察项、R27→有意保留），其余 32 条确认/读码确认；其中 R3/R4/R5/R7/R8/R17 为实测复现**

---

## 一、总览（优先级一句话）

> **一句话**：前七轮已把「真调能踩到」的崩溃 bug 清零；第八轮静态审查翻出的 17 条崩溃/异常逃逸中，**已实测复现 7 条**（R3/R4/R5/R7/R8 及读码确认的 R10/R13，另 R12/R23/R24 确认），这批是当前最高优先级——修法同前（入口/helper 加守卫一次收敛），单点改动、无需动主链路。其次是零风险三清、契约对齐、测试可信度；结构性（C1 两套队列）与接口合并收益最低、风险最高，排最后。

**ROI 阶梯**

| 梯队 | 做什么 | 为什么排这里 |
|---|---|---|
| **P0** | **已实测确认的崩溃/异常逃逸**：**R4**（GBK 文件 → `UnicodeDecodeError`）、**R8**（`csv_path` 父目录不存在 → `FileNotFoundError`）、**R5**（扫描中文件消失 → `FileNotFoundError`）、**R7**（turbocharts 超时 → `RuntimeError`）、**R3**（`launch_edi` 直 raise）、R10/R13（读码确认）+「入口加守卫」一次收敛 | 会穿透 MCP 边界、agent 拿到裸异常而非 `error_response`；**这 5 条已实测复现**（§十三），逐条修法与提交切分见 **§十四** |
| **P1** | **零风险三清**：N1 / B19 / B9（一个提交，无行为变更） | 顺手、零风险 |
| **P2** | **契约对齐**：T5（先统一失败信封）、T3-尾（两处拍板）、P2-5（`Out`/`EXCLUDED_TYPES`）、P1 参数命名、S1 别名过渡 | agent 每次调用都经过这一层，低风险局部改动 |
| **P3** | **测试可信度**：S4（`skipif` 分类，交付口径改 `N passed (M 真跑 / K skip)`）+ R28–R32 无效测试清理 | 不修则每轮「passed」不可信 |
| **P4** | **死代码 / 精简**：R21–R40、C4、N2 | 清复杂度，不影响使用者 |
| **P5** | **结构性 / 接口**：C1（两套队列）二选一、6.2 两组合并（带 alias） | 动主链路需回归，收益是维护者侧 |
| **不做** | ① 为「数量好看」合并工具（90→88 足够）；② 删「词典/公共 API/防御冗余」；③ 动真实信任边界（`report/generator` 校验链、`chat/routes` 上传限制、`_prepare_parameters` 校验） | 见 §九 明确不动项 |

---

## 二、崩溃 / 错误处理（本仓库侧，会穿透 MCP 边界）

> 第八轮静态读码发现 + **第九轮逐条复核（真调/读码，台账见 §十三）**。共性：信任边界处对 `None`/非 str/异常文件状态无守卫，抛原生异常而非 `error_response`。
> **已复核为误报、不再上报**：`R1`（`require_project_path(None/123)` 实测返回 `INVALID_PATH`「路径必须是字符串」，`validate_file` 已有 isinstance 守卫）、`R2`（`replace_port_component` 两个 None 实测返回 `EMPTY_INSTANCE_NAME`／`UNSUPPORTED_COMPONENT_TYPE`，`require_nonempty` 已守卫且写入前返回）。

| # | 位置 | 问题 |
|---|---|---|
| R3 | `eda/edi_launcher.py:48` | `launch_edi` 直接 `raise FileNotFoundError/ValueError/RuntimeError`，不像其它工具返回 `error_response` |
| R4 | `eda/project_reader.py:34` | `read_text` 严格 `utf-8` 无 `errors=` 回退，GBK 内容抛 `UnicodeDecodeError`；`signal_chain.py:31/46` 同场景用 `errors="replace"` |
| R5 | `eda/project_manage.py:70,221,226` | `epp.stat().st_size/st_mtime` 无守卫，broken symlink / 扫描中删除 → `FileNotFoundError` |
| R6 | `eda/signal_chain.py:29-33` | 存在但 0 字节的 `netlist.log` 短路到 `NETLIST_EMPTY`，不触发 gRPC 重导出兜底（**实测**：0 字节 → `NETLIST_EMPTY`「网表中没有器件/端口」；无该文件 → `NETLIST_NOT_FOUND`） |
| R7 | `turbocharts/convert_raw.py:664,729`、`compare_results.py:110` | `run_turbocharts` 超时抛 `RuntimeError`，两个调用点均未包 try（**实测**：`timeout=1` 跑 5s 子进程 → `RuntimeError: Turbocharts 执行超时（1 秒）` 裸逃逸） |
| R8 | `turbocharts/compare_results.py:87-90,192` | 只校验 `output_path` 父目录，`csv_path` 父目录未检查，不存在时 `open()` 抛 `FileNotFoundError` |
| R9 | `multimodal_vision/vision_analyzer.py:143` | `data.get("usage", {})` 后 `usage.get(...)`，provider 返回 `"usage": null` 抛 `AttributeError`，无 try/except |
| R10 | `start_servers.py:234` | `_grpc_cfg_addr.rsplit(":", 1)` 无守卫，`validate()` 只 warn 不拦，配置缺 `:` 启动 `ValueError` 崩溃 |
| R11 | `ansys/config.py:105` | `_attach_aedt` 兜底 ProgID 返回 desktop 对象本身，`GetAppDesktop()` 必抛，「多 ProgID 兜底」分支走不通 —— 🔸 **已降为观察项**（复核：`_COM_PROGIDS` 两项都会 `try`，失败抛的是文档化的 `RuntimeError`；「兜底是否真走不通」需 AEDT 环境才能定论，不在本仓库侧判定） |
| R12 | `ansys/config.py:150` | `get_project_lock_path` 用 `.with_suffix(".aedt.lock")`，`.aedtz` 工程算成 `demo.aedt.lock` 而非 `demo.aedtz.lock`，锁清理打错目标、且与同名 `.aedt` 工程**共用同一把锁**（**实测**：`get_project_lock_path("C:/demo.aedtz")` → `C:\demo.aedt.lock`） |
| R13 | `ansys/project_manage.py:227` | `taskkill` 用 `text=True` 但无 `encoding` → 与 T1 同类的解码风险（中文 Windows 的 GBK 输出 + UTF-8 模式）；返回码非 0 时报 `TASKKILL_FAILED`（`CLOSE_FAILED` 在 `:254`/`:311` 属其它路径，原描述「误报 CLOSE_FAILED」不准确） |
| R14 | `ansys/run_analysis.py:166` | 用 `pending_count()>0` 区分 `ANALYSIS_BUSY`/`TASK_LIMIT_REACHED` 不可靠 |
| R15 | `cst/cst_api.py:54` | `to_writable_copy` 用 `basename` 做临时名，两个工程同名互相覆盖模型/结果 |
| R16 | `cst/result_export.py:183` | VBA 字符串双写反斜杠（VBA 无转义），靠 Win32 折叠才碰巧工作 |
| R17 | `eda/model_library.py:234` | `_search_schematic` 不校验 `search_name` 非空，与 `_search_models`/`soft_ip.py` 不一致 |

---

## 三、资源 / 安全

| # | 位置 | 问题 |
|---|---|---|
| R18 | `process_guard.py:118` | `_cache[client_port]` 从不 evict，长跑无界增长（无状态 HTTP 每请求新临时端口） |
| R19 | `task_runner.py:192-201` | 「卡死任务」兜底只把 `status` 改 FAILED + 写 `finished_at`，**不删条目** → 容量（`len(self._tasks) >= max_tasks`）仍被占用到 TTL 过期。⚠️ 原文「执行槽未真释放」不准确：**全程序无信号量/执行槽机制**（无 `acquire`/`release`），是「最多阻塞 TTL（默认 2h）」而非「永远排队」 |
| R20 | `tr_simulation/client.py:52-54` | `requires_confirmation` 工具恒注入 `confirmed_by_user:True`，确认门空转（实质问题已交叉登记 `TR集成问题清单.md`，此处仅记位置） |
| P0-1 🔐 | EDI 侧 | `SimulationAgent.exe` 以 `--model-api-key sk-...` / `--edi-mms-api-token <JWT>` 明文 argv 启动（2026-09-15 复现），本机任何进程可读。**依赖侧**：修复在 EDI 侧（改环境变量/配置文件读取 + 启动后清零 argv），本仓库无硬编码密钥、`.env` 已 gitignore |

---

## 四、死代码 / 无效测试

| # | 位置 | 问题 |
|---|---|---|
| R21 | `chat/service.py:712-717` | 空串拒绝循环的名单与实际参数名不符（名单里 `component_id`／`instance_name`，实际多为 `component_uuid`／`instance_name`）；**`image_path` 已在名单内（原描述视作缺失，不准）**；`isinstance(val, str)` 使 `result_paths` 这类 list 参数永不触发 |
| R22 | `chat/service.py:237,762` | `last_projects` 只写不读 |
| R23 | `eda/grpc_client.py:384` | `action_accepted = response.code == 0` 恒真（`!=0` 已 :334 return） |
| R24 | `cst/cst_api.py:88` | `payload["message"] = snap["message"]` 后续分支全覆盖，赋值死代码 |
| R25 | `multimodal_vision/document.py:168` | `_doc_store.register` 的调用方（`register_document_url`／`open_document`）**总是显式传 `disposition`（默认 "inline"）**，故 `entry.get("disposition", default_disp)` 的业务默认值取不到 → `_mime_and_disposition` 里 `.docx→attachment` 那条永不生效（结论方向对，措辞改为「表的业务默认值被显式入参覆盖」） |
| R26 | `multimodal_vision/vision_analyzer.py:5` | 模块 docstring 与 `_encode` 实现（返回三元组）不符 |
| R28 | `tests/test_grpc_client.py:56-165` | 三用例只跑 Python 内建行为，从不 import 生产代码，永远通过 |
| R29 | `tests/test_turbocharts_runner.py:6-15` | Windows `echo` 非可执行文件，`FileNotFoundError` 被吞，静默空转 |
| R30 | `tests/test_tool_registry.py:67-74` | 只查本地 19998 端口空闲，与单实例逻辑无关 |
| R31 | `tests/test_bugfixes.py:136-140` | 断言硬编码常量 == 硬编码数字 |
| R32 | `tests/test_tool_index.py:9-19` | 渲染器又抄一份，与 `scripts/gen_tool_index.py` 漂移同源 |
| R33 | `scripts/build.ps1:82` | 删 COLLECT 版 spec 不产出的 exe，死删除 |
| R34 | `scripts/run.bat:10` | 硬编码 `v0.1.8` 版本号 |
| R35 | `scripts/win7/install_win7_env.py:95-98` | vcruntime/msvcp 缺失时仍报 PASS（应用 `warn()`） |
| R36 | `scripts/win7/diag_win7_exe.py:129,152` | 「清单已列出」「冒烟进程已结束」恒 PASS 步骤，虚增 checks 计数 |
| N1 | `process_guard.py:173-174` | 白名单放行分支残留两行注释掉的调试日志（`# _log.info(...)` / `# print(...)`） |

---

## 五、精简 / 一致性

| # | tag | 位置 | 问题 |
|---|---|---|---|
| R37 | 精简 | `eda/model_library.py:92-98` | `limit` 截断后 `count` 保留原总数（`{count:N, results:M<N}` 自相矛盾）；且 `limit` 只能缩小不能放大（服务端无分页参数，超过默认页是 no-op） |
| R38 | 精简 | `eda/soft_ip.py:60-67 / 88-95` | 两函数重复 `isinstance`+`call_grpc` 块，只差 `task_type`，可用 `model_library._search_models` 模式共享 |
| R39 | 精简 | `eda/project_manage.py:63` | `sorted(rglob)` 先物化全量再截断，应 `islice` 惰性化 + 防 `PermissionError` |
| R40 | 一致性 | `chat/service.py:807` | `result.get("success") is False` 与 `not result.get("success", True)` 两路径对假值处理不一致 |
| B7 | 精简 | `resources_prompts/` 7 文件 663 行 | 多为 `@mcp.prompt` + 静态 f-string，可合成 1 模块（7→3）；`test_mcp_content.py` 导入同步改。⚠️ §九 定为**不改**（按语义拆可读） |
| B9 | 精简 | `turbocharts/compare_results.py:93,186,197,200,206,214,216` | 写完文件立刻 `exists()` 求证、`success` 就是 `img_ok` 却复合判断、`dep_key = dependency` 无意义别名，≈6 行 |
| B19 | 流程 | `scripts/build.ps1` | 步骤号自相矛盾：`[1/7]…[7/7]` 之后又出现 `[8/8]` |
| C4 | yagni 半成品 | `eda/signal_chain.py` | `direction` 参数 `backward` 只把起点换成 load（`:234-236`），`_trace_chain` 未接 direction（`:246-248`），「向上游遍历」未实现却原样回显——「撒谎接口」，建议**删参数 + 删回显** |
| N2 | 精简 | `tr_simulation/tools.py:335-344` | MCP 层替上游补契约（`tr_query_components` 重排 + 补 null + 加 `missing`，≈10 行），条件 `not data or any(...)` 可读性差；真修复宜在上游，保留则加 `ponytail:` 注释标上限 |

---

## 六、结构性（需拍板）

| # | 问题 | 建议 |
|---|---|---|
| C1 | **两套并行任务队列**：`TaskRunner` 的 docstring 自认「后续逐步迁移到本类」，但 `eda/simulation.py:48-79` 仍是完整一套平行实现（`_sim_tasks`+`_sim_lock`+`_SIM_EXECUTOR`+`_prune_tasks_locked`+`_TASK_TTL`）；实测 HFSS/CST 共用 `TaskRunner` | 二选一：eda 改用 `TaskRunner`，或删 `task_runner.py` 各留各的。⚠️ §九 定为**现阶段不合并**（要先给 `TaskRunner` 加进度回调、动仿真主链路，风险 > 收益） |
| C2 | `tr_simulation/tools.py`（345 行，17 个 `@mcp.tool()`）1:1 镜像上游 schema，默认值逐字抄上游（与 `MCP_WORKFLOW.md` 各存一份） | **不建议删 wrapper**（docstring 即 tool description）。改为加校验：启动/CI 拉 `/api/v1/integration/tools` 比对参数名与默认值，漂移即失败 |

> **已定性保留（不再重复上报）**：**C3** `ProcessProbeMiddleware` 调试探针（排查客户端 403/连不上反查来源进程）；**C5** `eda/__init__.py` 的 56 个 re-export（`README.md:119` 文档化公开 Python API）；**A16** `TaskRunner.count()/prune()`（通用类公共 API）；**A3** `ERROR_CODES` 中 `_tool_error` 未直接用到的几条（错误消息词典，查表完整性）。

---

## 七、契约 / 命名 / 文档

| # | 问题 | 证据 / 说明 |
|---|---|---|
| P1-1 | 「型号/ID 列表」参数名不统一 | `batch_query_component.model_name_list`（仅型号串）／`tr_query_components.originalid_list`（UUID 或 model_name）／`get_components_static_params.original_uuids`（真 UUID）三处仍不统一 |
| P1-2 | `originalid` 命名残留 | `tr_simulation/tools.py` 用 `originalid_list`（无下划线），建议对齐 `batch_query_component` |
| P1-3 | 工程路径多名字 | `list_epp_projects.folder_path` vs `create_project.path` vs 多数 `project_path` vs tr_* `epp_path`（镜像 SimulationAgent 契约，保留） |
| P2-1 | docstring 格式不统一 | 多数「用法:」中文体，少数「Args:」英文体（`close_edi_project`、`capture_schematic`、`simulate_netlist_with_ads` 等） |
| P2-2 | 返回字段键语言不一致（直调复核仍成立） | `get_components_static_params.details.data[]` 键为中文（`封装/尺寸/成本/所属厂商/模型id/重量`），`batch_query_component` 键为英文（`description/manufacturer/model/specs/type`）。根因在 EDI 返回体，仓库侧可做映射层 |
| P2-3 | 列器件工具信封不一致 | `list_schematic_components` 是 gRPC 任务信封（`details.component_count`）无分页；`list_simulation_components` 扁平结构（`total/offset/limit/component_type_counts`）带分页；`tr_query_schematic_components` 又是 TR 信封 → 基础层保留统一返回 + 分页，TR 版 docstring 点明「非 TR 流程用 `list_simulation_components`」 |
| P2-4 | 协议方法暴露为「伪工具」（备忘，非 bug） | `get_prompt`/`list_prompts`/`list_resources`/`read_resource` 是 MCP 协议方法非工具，服务端实测可用（`prompts/list` 9、`resources/list` 8）；Hermes 会话快照把它们暴露成 4 个「伪工具」——纯客户端展示问题，服务端修不了 |
| P2-5 | CSV 导出漏 `Out` + docstring 指向不存在的常量（仍复现） | ① `export_schematic_components_to_csv`（工程 23）输出仍含 `Out,Out1,,,,` 一行，而 `TermG`/`SParameter`/`Sweep`/`Var`/`HarmonicBalance`/`XDB`/`P_nToneG` 均已排除 → 按「端口不导出」口径 `Out` 也应入排除表（排除表在 EDI 侧）；② `design_export.py` docstring 提到 `EXCLUDED_TYPES` 但代码无此标识符——措辞需订正 |
| T5 | **两套返回信封** | EDA gRPC 家族业务数据在 `details`（带 code/hint）；本地/TR 家族直接放顶层且 `details=null`（实测：`get_project_summary`、`analyze_variables`、`get_signal_chain`、`list_simulation_components`、`list_eda_tasks`、`list_result_curves`、`get_simulation_component_schema`、`tr_*` 全家族）。收敛方案见 §八 |
| T3-尾 | 两处待拍板 | ① `create_project` 的 `path` 父目录无本地校验；② `switch_workspace` 现要求目录必须已存在（若原语义允许「指向未来目录、下次启动创建」则属收紧）。倾向见 §八 |

---

## 八、待你拍板（我给倾向 + 理由）

1. **T3-尾 ① `create_project` 的 `path` 要不要本地校验？** → 倾向**要但只做最小校验**（非空时校验父目录存在/可写，不存在 `INVALID_PATH`，不新建目录）；或退一步——不校验但把语义写进 docstring。理由：`create_workspace` 已因「垃圾值真写入」挨过一次（T3）；传错路径时你想要「报错」还是「默默在 `C:/` 建工程」？
2. **T3-尾 ② `switch_workspace` 要求目录已存在？** → 倾向**保留收紧**。理由：EDI 侧实测「指向不存在目录」时根本没落文件，本地拒绝只是更早暴露问题；唯一风险是 EDI 将来支持「指向未来目录」会挡路，那就 docstring 写明「目录必须已存在」。
3. **C4 `direction` 定性** → 倾向**删参数 + 删回显**，不补实现。理由：现在对 agent 是「撒谎接口」（以为拿到反向链路）。若确需反向遍历：当 bug 修（`_trace_chain` 接 direction + 反转邻接），且**必须补测试**。
4. **T5 两套信封 → 分两步**：① 先统一「失败」信封（所有家族失败一律 `error_response(...)`，ANSYS 已迁完）→ agent 判失败只看 `success`；② 「成功」信封暂不合并——EDA gRPC 家族 `details` 带 `status/task_success/outcome_known/hint`，本地/TR 家族没有任务语义，强行统一只会造出「永远为 null 的字段」，改为 docstring 首行写清「本地计算，不产生 task 信封」。
5. **S1 契约口径 → 用别名过渡，别一步改签名**：先 `originalid_list` 加别名 `original_ids`（两者都收）→ docstring 只写新名 → 下轮删旧名。直接改签名会打断现有会话/脚本。
6. **R11 定性 → 倾向降为「观察项」**：`_COM_PROGIDS` 两项都会 `try`，失败抛的是文档化的 `RuntimeError`（不是崩溃）；「多 ProgID 兜底是否真走不通」只能在 AEDT 环境实证，本仓库侧判不了。若你手边正好有 AEDT 未运行/已运行两种状态，测一次 `query_desktop_state()` 就能定案。
7. **扫描范围口径 → 已按实测改为「98 个 .py / 全仓 180 文件 / 18,890 行」**（原「133 文件」口径不明）；若你记得 133 的口径（例如含 docs 的 `.md`），告诉我我改回更准确的表述。

---

## 九、明确不动项 + 测试可信度

### 9.1 明确不动（已逐条定性）

| 不做 | 理由 |
|---|---|
| 为「数量好看」合并工具（90→88 够，别追 50） | §6.1 三条判据；判据 3（不产生互斥可选参数）本身会挡住大多数 |
| 删「词典/公共 API/防御冗余」（`ERROR_CODES`、`TaskRunner.count/prune`、`eda/__init__` re-export、`load_dotenv` 重复、`check_pyd_imports.py` 手写 PE 解析） | 查表完整性 / 对外契约 / 安全冗余 / 刻意零依赖 |
| C1 现在合并两套任务队列 | 先给 `TaskRunner` 加进度回调、动仿真主链路，风险 > 收益 |
| C2 删 17 个 TR wrapper | docstring 就是客户端 tool description |
| B7 合并 `resources_prompts` 7 文件 | 口味不是问题，按语义拆可读 |
| 动真实信任边界（`report/generator` 校验链、`chat/routes` 上传限制、`_prepare_parameters` 校验） | 这些「冗余」是安全边界，不是过度工程 |
| 只改测试口径数字、不做 `skipif` 分类 | 只把 413 写成「含跳过」没用，要真加分类才能分辨真跑 |
| 删 `execution_mode` 参数（原 R27） | docstring 已注明「已废弃（仿真仅支持异步），保留参数兼容旧调用」——**文档化的兼容参数**，不是死代码 |

### 9.2 测试可信度（S4，最高性价比）

- **现状**：「真跑」与「跳过」混在同一个 `passed` 计数里，**413 passed 里有多少真跑无法分辨**，这是审计可信度的根。
- **做法**：① `tests/conftest.py` 统一 fixture + `skipif(not HAS_EDI)`／`skipif(not HAS_PROJECT)` 显式分类；② 新增「工具数 == 清单」「白名单 403 路径」断言；③ `scripts/win7/diag_win7_exe.py` 区分「本机 UCRT 垫片」与「真实依赖」。
- **交付口径**：改 `N passed (M 真跑 / K skip)`。
- **顺手清无效测试**：R28（test_grpc_client 空跑）、R29（echo 静默）、R30（端口无关）、R31（常量==常量）、R32（渲染器重复）。
- **收尾**：`list_schematic_components` 的 `summary_only`/`limit` 补 1 条 mock 单测。（S3 清单自动生成、S5 统一返回契约已闭环）

---

## 十、接口收敛路线（工具合并，降认知成本）

> 结论均基于**真实签名**（`inspect.signature`，2026-09-16）。

**合并判据（三条同时满足才合并）**：① 后端同一个（同一 gRPC action / HTTP 端点）；② 参数集高度对称（重叠 ≥ 80%）；③ 合并后不产生互斥可选参数。

| 候选 | 结论 | 依据 |
|---|---|---|
| `use_schematic_from_library_create_project` + `..._import` | ✅ **合并** → `use_schematic_from_library(file_uuid, mode, project_path="")` | 只差一个参数，选错有后果（新建 vs 导入） |
| `simulate_netlist` + `simulate_netlist_with_ads` | ✅ **合并** → `simulate_netlist(netlist_path, ads_path="", ...)` | 只差 `ads_path`；timeout 默认值不一致（600 vs 120）正好统一 |
| `search_public_models` + `search_personal_models` | 🔸 不合并，只 docstring 互指 | 签名完全一致但判据 3 不满足：合成 `library=` 会静默取错源 |
| `search_schematic_*_public` + `..._personal` | 🔸 不合并 | 同上 |
| `add_ideal/perf/create_simulation_component` | ❌ 不合并，改命名族化 | 数据源与必填项各异，合并成互斥可选参数巨型工具 |
| `list_schematic_components` / `list_simulation_components` | ❌ 不合并，但参数对齐 | 前者无 offset，后者有；以后者为模板统一 |
| `export_project_netlist` / `tr_get_project_netlist` 等 | ❌ 不合并 | TR 版是版本化可追溯工作流，重叠是故意的，docstring 互指 |
| `get_simulation_async_status` / `..._result` | ❌ 不合并 | 一个是轮询、一个取大 payload |
| `open/close_edi_project` vs `open/close_hfss_project`；`cst_*_query` vs `get_hfss_*` | ❌ 绝不合并 | 不同工具链后端；真问题是命名漂移 |

**净结果 90 → 88（只减 2）**。若执行合并：属 breaking change，保留旧名 alias 过渡；同步点 `scripts/gen_tool_index.py`（重生成 `TOOL_INDEX.md`）、`tests/test_tool_index.py`、`docs/TOOLS_API.md`、`chat/index.html` 工具映射。

**比合并更划算的三件事**：① 命名族化（`add_*`/`list_*`/`tr_*` 统一前后缀，docstring 首行写清来源/后端）；② 参数语义对齐（列/搜类统一 `limit`/`offset`/`summary_only`）；③ `TOOL_INDEX.md` 分组渲染（已自动生成）。

**待优化（保留但改进）**：异步查询命名三套不一致（CST `*_query` / HFSS `get_*_status` / EDA `get_*_async_status`；`cst_export_snp` 无 query、`cst_export_farfield` 有 query）；模型搜索仍无 `offset`（软 IP `search_*_soft_ip_models` 已由服务端自动合并分页，可作参照）。

---

## 十一、依赖侧 / 环境（本仓库改不了，仅跟踪）

| # | 问题 | 说明 |
|---|---|---|
| P0-1 | SimulationAgent 密钥明文 argv | EDI 侧修复（§三） |
| T9 | `analyze_image` 当前不可用：视觉模型请求 `SSL: UNEXPECTED_EOF_WHILE_READING` | 疑端点/证书/网络。先分 `VISION_BASE_URL` 可达性 + 证书链各测一次；注意 `analyze_image`（`VISION_*`）与 `show_image`（`IMAGE_ALLOWED_DIRS`）是两套独立配置 |
| N4 | EDI GUI 的 ADS 路径：`config.ini` `[ads] hpeesof_dir` 现值正确，但仍需 GUI 设置同指，否则 EDI 启动重写后复现 | 备份 `config.ini.bak-20260910` |
| N6 | Hermes 客户端旧服务器名 `edi-mcp-sse-50026` 残留 `state.db`（114 次）/`profiles/0/state.db`（46 次）/`cache/mcp_schema_cache.json`，每轮重启多一次无谓连接 | `hermes mcp list` 只有 `eda` 一个，配置本身干净 |
| P2-2/3/5 根因 | EDI 返回体键语言、`Out` 排除表 | 仓库侧做映射层/措辞订正（§七），根因在 EDI 侧 |

---

## 十二、审计方法论（供下轮复用）

1. **别用 Hermes 的 shell 环境判定「编码/环境类修复」**：Hermes 工具 shell 自带 `PYTHONUTF8=1`+`PYTHONIOENCODING=utf-8`+`PYTHONPATH=<hermes>`，会把依赖 locale 的修复判成失败。判定法：`env -u PYTHONUTF8 -u PYTHONIOENCODING` 再跑对照，两种环境结论不同 = 修复绑在环境上，本身就是 bug 信号。
2. **判定「中文是否可读」用布尔 + 落文件，别信屏幕文字**：清掉 `PYTHONIOENCODING` 后 stdout 变 cp936，重定向回 UTF-8 管道会把正确中文显示成乱码。
3. **每类探针用互不干扰的独立路径**：曾被 `create_workspace` 先建出的目录掩盖过 `list_epp_projects` 抛异常（「文件夹不存在」探针假通过）。
4. **探针 helper 形参别叫 `name`/`path`**：与工具参数撞名会在调用处 `TypeError`，用 `tool` 等中性名。
5. **「看起来像」的结论一律真调复核**：多轮改判（`search_*` 不合并、`limit` 静默失效、`compare_simulation_results` 从未可用、T13 从「已修」改「半修」）全来自实测而非阅读。**第八轮静态扫的 R 系列已按此逐条复核（§十三）：40 条里 2 条误报、5 条描述不准**（R19「永远排队」实为「最多阻塞 TTL」、R21 名单其实含 `image_path`、R13 误报的 `CLOSE_FAILED` 其实在别的路径……）——**子代理读码的命中率约 90%，但那 10% 会误导修复方向**。另：能真调的一律真调（broken symlink 造 `R5`、GBK 文件造 `R4`、`timeout=1` 造 `R7`、坏目录造 `R8`），比读码强得多。

---

## 十三、第八轮 R 系列逐条复核台账（2026-09-17）

> **处置建议（怎么修/改哪个文件/能否一批提交）见 §十四。**
> **口径**：静态读码结论一律当自述，逐条复核后才定稿——**能真调的一律真调**（临时目录、不碰真实工程），不能真调的读码比对。结论：✅确认 / 🔸待实证 / ⚠️描述订正 / ❌误报。

| # | 结论 | 复核证据（2026-09-17） |
|---|---|---|
| R1 | ❌ **误报** | 实测 `project_path=None` / `=123` → `INVALID_PATH`「路径必须是字符串」；`validate_file` 已有 isinstance 守卫 → 已从 §二 删除 |
| R2 | ❌ **误报** | 实测 `replace_port_component(target_instance_name=None, replacement_component_type=None)` → `EMPTY_INSTANCE_NAME` / `UNSUPPORTED_COMPONENT_TYPE`（`require_nonempty` 守卫，写入前返回）→ 已从 §二 删除 |
| R3 | ✅ 确认 | 实测 `launch_edi(edi_path="C:/__verif_nope__/EDI.exe")` → 裸 `FileNotFoundError` 逃逸 |
| R4 | ✅ 确认 | 实测：GBK 编码 `netlist.log` → `ProjectReader.read_text` 抛 `UnicodeDecodeError`（`0xd6` 非法续字节）；同场景 `get_signal_chain` 用 `errors="replace"` 正常 success（两处口径不一致） |
| R5 | ✅ 确认 | 实测：扫描目录放 broken symlink → `list_epp_projects` 抛 `FileNotFoundError [WinError 2]` |
| R6 | ✅ 确认（措辞已订正） | 实测：0 字节 `netlist.log` → `NETLIST_EMPTY`（不走 gRPC 兜底）；无该文件 → `NETLIST_NOT_FOUND` |
| R7 | ✅ 确认 | 实测 `run_turbocharts(5s 子进程, timeout=1)` → `RuntimeError: Turbocharts 执行超时（1 秒）`；两个调用点均未包 try |
| R8 | ✅ 确认 | 实测 `csv_path` 父目录不存在 → `FileNotFoundError` 逃逸（`output_path` 有校验、`csv_path` 没有） |
| R9 | ✅ 读码确认 | `usage = data.get("usage", {})` 后 `usage.get(...)`：provider 返 `"usage": null` 时 `get` 返回 `None` → `AttributeError`（无守卫） |
| R10 | ✅ 读码确认 | `_grpc_host, _grpc_port = _grpc_cfg_addr.rsplit(":", 1)`：addr 缺 `:` → unpack `ValueError`（启动期） |
| R11 | 🔸 待实证 | `_COM_PROGIDS = ("AnsoftHfss.HfssScriptInterface", "Ansoft.ElectronicsDesktop")` 两项都 try，失败抛文档化的 `RuntimeError`——「兜底走不通」需 AEDT 环境才能证伪/证实，建议降为**观察项** |
| R12 | ✅ 确认 | 实测 `get_project_lock_path("C:/demo.aedtz")` → `C:\demo.aedt.lock`：`.aedtz` 被截成 `.aedt`，**且与同名 `.aedt` 工程共用同一把锁** |
| R13 | ✅ 确认（影响订正） | `taskkill` 用 `text=True` 无 `encoding` → 与 T1 同类解码风险；但返回码非 0 报 `TASKKILL_FAILED`（`CLOSE_FAILED` 在 `:254`/`:311`，属其它路径） |
| R14 | ✅ 读码确认 | `pending_count()>0` 区分 BUSY/LIMIT 不可靠：达上限且有 pending 时报 `ANALYSIS_BUSY` |
| R15 | ✅ 读码确认 | 临时副本名用 `basename` → 同名不同目录的模型互相覆盖（TTL 窗口内） |
| R16 | ✅ 读码确认 | `output_dir.replace("\\","\\\\")` 在 VBA 里无意义（VBA 无反斜杠转义），靠 Win32 折叠重复分隔符碰巧工作 |
| R17 | ✅ **确认（实测，影响比描述更重）** | 实测 `search_schematic_from_public_library(search_name="")` 与 `=None` 均 → `success=False`、**`error_code=None`**、message = 「网络错误: server replied: Bad Request」——空 `search_name` 触发上游 400，却被包成**「网络错误」且无错误码**（比「缺校验」更误导：agent 会以为网络问题）。代码里 `{"search_name": search_name or ""}` 未做本地非空校验 |
| R18 | ✅ 读码确认 | `_cache[client_port]` 只有 get(`:113`)/set(`:118`)，无 evict → 无界增长 |
| R19 | ⚠️ 描述订正 | **全程序无信号量/执行槽**（无 `acquire`/`release`）；容量 = `len(self._tasks) >= max_tasks`，卡死任务不删条目 → 占容量到 TTL（默认 2h）过期，是「最多阻塞 2h」而非「永远排队」 |
| R20 | ✅ 读码确认 | `_confirm_id()` 恒返回非空 `mcp-confirm-<ts>`，确认门默认通过 |
| R21 | ⚠️ 描述订正 | 名单里**已有** `image_path`（原描述视作缺失，不准）；真正不符的是 `component_id`（实际多叫 `component_uuid`）；`isinstance(val, str)` 使 `result_paths` 永不触发 ✓ |
| R22 | ✅ 确认 | grep：`last_projects` 仅声明 `:237` + 赋值 `:762`，零读取 |
| R23 | ✅ 确认 | `grpc_client.py:334` 的 `if response.code != 0:` 分支内 `return finish(False, "REJECTED", …)` → `:384 action_accepted = response.code == 0` **恒真** |
| R24 | ✅ 确认 | `cst_api.py:83-88` 先赋 `payload["message"]=snap["message"]`，随后运行中/成功两分支都重新赋值并 `return` → 首赋为死代码 |
| R25 | ⚠️ 描述订正 | 调用方总是显式传 `disposition`（默认 `"inline"`）→ `entry.get("disposition", default_disp)` 的业务默认值取不到，`.docx→attachment` 那条永不生效 |
| R26 | ✅ 确认 | 模块 docstring 写「成功 (data_url, None)」，而 `_encode` 实为三元组（函数自身 docstring 是三元组） |
| R27 | 🔸 **改为「有意保留」** | docstring 已注明「已废弃（仿真仅支持异步），保留参数兼容旧调用」→ 文档化兼容参数，已移入 §九「明确不动」 |
| R28 | ✅ 确认 | `TestLogAccumulation` 只拼字符串、不 import 生产代码 |
| R29 | ✅ 确认 | `run_turbocharts(["echo","test"])`：Windows 无 `echo.exe` → `FileNotFoundError` → `pass` 静默通过 |
| R30 | ✅ 确认 | `test_single_instance_check` 只 `socket()` 探 19998 端口空闲，与单实例逻辑无关 |
| R31 | ✅ 读码确认 | 断言「硬编码常量 == 硬编码数字」 |
| R32 | ✅ 读码确认 | 测试内自渲染一份，与 `scripts/gen_tool_index.py` 同源漂移 |
| R33 | ✅ 确认 | `build.ps1` 同时出现 `[7/7]` 与 `[8/8]` |
| R34 | ✅ 确认（已精确） | `scripts/run.bat:10` 硬编码 `v0.1.8`，而 `pyproject.toml` 与 `servers/__init__.py:26` 均为 **0.1.9** → 落后一版 |
| R35 | ✅ 确认 | `step("系统里有 %s…", True, "已装" if p.exists() else "未装…")` 第 2 参恒 `True` → DLL 缺失也 PASS |
| R36 | ✅ 确认 | `step("清单已列出", True)`／`step("冒烟进程已结束", True, …)` 恒 PASS，虚增 checks 计数 |
| R37 | ✅ 确认 | `bucket[:] = bucket[:limit]`（注释自认保留 `count`）→ `{count:N, results:M<N}`；`limit` 只能缩小 |
| R38 | 🔸 未逐行复核 | 两函数同构重复，读码上明显；未做逐行比对 |
| R39 | ✅ 确认 | `sorted(root.rglob("*.epp"))` 先物化全量再 `[:1000]`；无 `PermissionError` 处理 |
| R40 | ✅ 读码确认 | 同一函数内 `result.get("success") is False` 与 `not result.get("success", True)` 两种假值口径并存 |

**全局口径核对**：工具数 90 ✓、资源 8 / Prompt 9 ✓、套件 `413 passed / 1 skipped` ✓；⚠️ **「133 文件 / ~18.7k LOC」的 LOC 对得上**（实测 18,890 行），但**文件数口径不符**——实测 `servers/`+`tests/`+`scripts/`+`start_servers.py` 的 `.py` 为 **98 个**、全仓（排除 `.venv`/`build`/`dist`/`__pycache__`）**180 个文件**。建议改为「98 个 .py（全仓 180 文件）/ 18.9k 行」并注明口径。

---

## 十四、R 系列处置建议（可执行修法 + 明确不改）

> 与 §十三 台账配套：**怎么修**、**改哪个文件**、**能不能一批提交**。原则沿用前几轮——**在入口/helper 处一次收敛**（`require_*` 家族 + 统一 `error_response`），而不是 40 处 try/except。

### 14.1 P0 五条（已实测复现，建议第一批修）

| # | 修法 | 改动量 |
|---|---|---|
| **R4** | `ProjectReader.read_text` 加解码回退：**别再依赖 locale**（T13 的教训）——抽一个 `servers/utils.py` 的 `read_text_fb(path)`（`try utf-8 → except UnicodeDecodeError 用 cp936 → 兜 errors="replace"`），`project_reader.read_text` 与 `signal_chain._acquire_netlist` **共用同一个**（消除现在两处口径不一致） | 新增 1 helper + 2 处改调用 |
| **R8** | `compare_results` 里把 `output_path` 那段父目录校验复制给 `csv_path`（复用现成的 `OUTPUT_DIRECTORY_NOT_FOUND`），或抽 `_require_parent_dir(path, label)` 校验两者 | 3–5 行 |
| **R5** | `list_epp_projects`：`epp.resolve()/stat()` 包 `try/except OSError` → 跳过该文件并在结果里带 `skipped`；`rglob` 同接 `PermissionError`（与 R39 一起做） | ~8 行 |
| **R7** | 两个调用点（`convert_raw.py:664,729`；注意 `:729` 那处是 `try/finally`，`finally` 不吞异常）包 `except RuntimeError as e: return error_response("TOOL_TIMEOUT", str(e))`；更 DRY 的做法：在 `turbocharts/config.py` 再加一个返回 `(proc 或 None, error 或 None)` 的包装，对齐 `require_file` 家族 | 2 处调用点或 1 个包装 |
| **R3** | `launch_edi` 三处 `raise` 改 `error_response`：`FileNotFoundError → EDI_NOT_FOUND`、`ValueError → INVALID_PATH`、`RuntimeError → LAUNCH_FAILED`（与其它 EDA 工具对齐） | 3 行 |

### 14.2 读码确认类（同一批顺手，都是小改）

| # | 修法 | 备注 |
|---|---|---|
| **R10** ⚠️ 优先 | `start_servers.py:234` 的 addr 解析加守卫：`rpartition(":")` + 端口必须是数字，失败**明确报错退出**（现在是 `ValueError: not enough values to unpack` 崩在启动期）——**它决定服务能不能起来**，实际优先级高于表里的位置 | 建议在 `validate()` 里拦（现在只 warn） |
| **R13** | `taskkill` 的 `subprocess.run` 用 14.1 抽出的解码 helper（`servers/utils.py`），与 turbocharts 的 `_decode_console` 合并成一个 | 顺手消除重复 |
| **R12** | `Path(project_path).with_suffix(".aedt.lock")` → `Path(str(project_path) + ".lock")`：`.aedt → demo.aedt.lock`、`.aedtz → demo.aedtz.lock`，两把锁不再互踩 | 1 行 |
| **R9** | `usage = data.get("usage") or {}`（`"usage": null` 时不再 AttributeError） | 1 行 |
| **R17** | 两件事：① `search_name` 与 `_search_models`/`soft_ip` **统一**（要么都允许空、要么都 `require_nonempty`）；② **更要紧的是那个包装**——空参数触发上游 400 却报「网络错误」且 `error_code=None`，建议把上游 4xx 映射成 `INVALID_PARAMETERS`（属 §七 契约类，不只是 R 项） | 1 处 + 契约 |
| **R15** | 临时副本名加路径指纹：`f"{hashlib.md5(model_path.encode()).hexdigest()[:8]}_{basename}"`，避免同名工程互覆 | 2 行 |
| **R16** | 删掉 `output_dir.replace("\\", "\\\\")`（VBA 无反斜杠转义） | 1 行 |
| **R18** | `_cache` 加「按数量/时间清理」（保留现成的 TTL 判定，补一次 prune 调用即可） | ~5 行 |
| **R19** | 容量判定改为只数**未结束**任务：`sum(1 for t in self._tasks.values() if t.get("finished_at") is None) >= self._max_tasks`——卡死任务标记 FAILED 后**槽位立即释放**，同时保留 FAILED 记录供查询（比删条目好） | 1 行 + 注释订正 |
| **R14** | 保持现状 + 注释说明「`pending_count()>0` 只能近似区分」；若要精确，`require_idle` 直接返回失败原因 | 注释级 |
| **R22/R23/R24/R26** | 一行级删除/订正：`last_projects` 死字段、`action_accepted` 恒真（或直接删变量）、`cst_api` 首赋 `payload["message"]`、`vision_analyzer` 模块 docstring 改三元组 | 各 1–2 行 |
| **R25** | 只改注释措辞（「disposition 由业务覆盖表决定」→「仅当调用方未显式指定时生效」）；**别动代码** | 注释级 |
| **R37** | `count` 语义二选一：建议照 `search_*_models` 的既有实现——**`count` 保原总数 + `details.returned` 标实际条数**；并在 docstring 注明「`limit` 大于服务端默认页时是 no-op」 | ~3 行 |
| **R39** | `sorted(root.rglob("*.epp"))` → `itertools.islice(root.rglob("*.epp"), _MAX_PROJECTS)` 惰性化 + `PermissionError` 兜底（与 R5 同批） | ~5 行 |
| **R40** | 统一假值口径（建议一律 `result.get("success") is False`） | 1 行 |
| **R38** | 抽共享 helper（仿 `_search_models`），**低优先**，除非正好在改软 IP | — |
| **R33/R34** | `build.ps1` 步骤号统一；`run.bat` 的 `v0.1.8` 改为不写版本、或由生成脚本注入（现在落后实际 0.1.9 一版） | 2 行 |
| **R35/R36** | `step(name, True, ...)` 的第 2 参换成**真实条件**：「清单已列出」删掉或标为信息项；DLL 检测改为 `p.exists()` 参与判定（否则缺 DLL 也 PASS） | 3–4 行 |

### 14.3 测试类（与 S4 一起做，别单独修）

**R28/R29/R30/R31/R32 这批「永远通过」的测试比没有测试更糟——它们虚增 `passed` 计数**。建议：能改成真实断言的改（R28 直接 import 生产函数；R31 换成行为断言），不能改的**删掉**（R29 的 `echo` 静默、R30 的端口探测）。配套 §九 9.2 的 `HAS_EDI`/`HAS_PROJECT` 分类，交付口径改 `N passed (M 真跑 / K skip)`。

### 14.4 明确不改 / 已降级

| 项 | 处置 |
|---|---|
| **R11** | 降为**观察项**（需 AEDT 环境实证，本仓库侧判不了；失败路径本身是文档化的 `RuntimeError`） |
| **R27** | 已移入 §九「明确不动」——文档化的兼容参数 |
| **R25** | 仅改注释，不动逻辑（业务覆盖表的默认值本就该被显式入参覆盖） |
| **R14** | 仅加注释（精确区分 BUSY/LIMIT 需改 `TaskRunner` 接口，收益低） |
| **R38** | 低优先，等改软 IP 时顺手 |

### 14.5 建议的提交切分（三批，互不阻塞）

1. **入口守卫批**（R3/R4/R5/R7/R8 + R9/R10/R12/R13）—— 都是「喂坏参数/坏环境 → 裸异常」，共用 1 个新 helper（`read_text_fb`）+ 2 处 helper 复用
2. **死代码 / 脚本批**（R14/R15/R16/R18–R26/R33–R40，除 §十三 已标「明确不改/降级」的）—— 单点小改
3. **测试批**（R28–R32 + S4 + S5 收尾）—— 与 `conftest.py` 分类一起做，改完交付口径就能写 `M 真跑 / K skip`

---

## 附：证据留档（仅未修项）

`export_schematic_components_to_csv`（P2-5：`Out` 仍漏导，工程 23）：

```text
original_model_type,original_model_name,original_model_id,alternative_model_type,alternative_model_name,alternative_model_id
Out,Out1,,,,
NC10355C_2931,NC10355C_29311,36742914-1237-4530-baef-488e4765a920,,,
```

`get_components_static_params` vs `batch_query_component`（P2-2：键语言不一致）：

```text
get_components_static_params.details.data[0] 键: ['封装', '尺寸', '成本', '所属厂商', '模型id', '重量']
batch_query_component.details.data[0] 键: ['description', 'manufacturer', 'model', 'specs', 'type']
```
